# JR Jewellers — Build Task Tracker

Shared checklist for the Next.js rebuild. **Companion to `ARCHITECTURE_PLAN.md`** (the spec)
and `CLAUDE.md` (repo guide). We take these **one at a time, top to bottom**.

**Status legend:** ⬜ todo · 🟡 in progress · ✅ done · ⏸️ deferred

## Ground truth (as scaffolded)
- **Stack:** Next.js `16.2.9` (App Router) · React `19` · Tailwind `v4` · TypeScript · **Bun** (`bun.lock`).
- **Layout:** app lives at **root `app/`** (not `src/` — decided override of plan §4). Path alias `@/*` → `./*`.
  **Single-source-of-truth registries** (no per-file link/value duplication): `lib/routes.ts` (`ROUTES` — every URL + `category(slug)`/`product(slug)` builders; change a scheme here only), `lib/navigation.ts` (`PRIMARY_NAV`, `FOOTER_SHOP_LINKS`, `FOOTER_HELP_LINKS` built from `ROUTES`), `lib/theme.ts` (`PLACEHOLDER_GRADIENT`). All link/href usages go through these.
  Follows ARCHITECTURE_PLAN §4 otherwise: routes under **`app/(storefront)/`** (group scopes the customer Header/Footer + settings fetch via `(storefront)/layout.tsx`; admin gets its own group in Phase 3); category is the **bare `[category]` segment** → `/{slug}` URLs (plan §4; note: bare slugs share the root namespace with static routes like `/shop`, `/cart` — Next resolves static before dynamic). Components grouped **by surface**: `components/storefront/{layout,product,home}` (+ `ui/`, `admin/` when needed). `stores/`, `styles/` split deferred (YAGNI); `db/` is Supabase (`lib/db`), not Prisma.
- **Backend:** Supabase project `jr-jewellers` (`naolegptozpaiojozzcy`, ap-south-1). Schema + seed
  already applied; storefront tables are **RLS public-read**. Env in `.env.local`.
- ⚠️ **Next 16 has breaking changes** — read `node_modules/next/dist/docs/` before writing route/server code (per `AGENTS.md`).
- **Scope rules:** storefront-first · **COD-only for v1** · Razorpay/Shiprocket deferred.

---

## Phase 0.0 — DB hardening ✅ done
Migration `storefront_scale_hardening` applied & verified. Production-readiness for reads/writes at scale:
- ✅ `updated_at` + auto-update triggers on `product`, `product_image`.
- ✅ CHECK constraints on `product.status`, `product.badge`, `review.status` (protects partial indexes from typos).
- ✅ One-primary-image guarantee (`product_image_one_primary` partial unique index) + dedup.
- ✅ Denormalized `product.primary_image_url` + auto-sync trigger (join-free listing reads). *Currently NULL — seed images are gradient placeholders (`bg`), `url` not set; populates automatically once real photo URLs land.*
- ✅ Denormalized `rating`/`review_count` kept honest via `review`-change trigger.
- ✅ Full-text search: generated `product.search` tsvector + GIN index (replaces slow `ILIKE`).
- ✅ Query-pattern partial indexes: `product_active_by_cat`, `product_featured_idx`.
- Deferred (YAGNI): keyset pagination is a query change (indexes ready); `order`/`order_item` indexing → design in Phase 2; partitioning/replicas → not until ~100k+ rows.

## Phase 0 — Foundation & cleanup
Goal: a clean, branded shell wired to Supabase. No product pages yet.

- ✅ **0.1 — Strip scaffold leftovers.** Renamed package → `jr-jewellers`; deleted `CLAUDE copy.md`, `.DS_Store`, 5 sample SVGs; minimal placeholder `app/page.tsx`. *(Default `layout.tsx` metadata handled in 0.5.)*
- ✅ **0.2 — Folder convention.** Root `app/` + `lib/db`, `lib/utils`, `components/{ui,product,layout}` (alias `@/*`).
- ✅ **0.3 — `git init` + baseline commit.** On `main`; `.env.local`/`node_modules`/`.next` confirmed excluded. Commit `3ce9c46`.
- ✅ **0.4 — Brand design tokens.** `app/globals.css` rewritten: maroon/gold/cream palette, `--font-*`, sharp radii via Tailwind v4 `@theme`; forced dark mode removed.
- ✅ **0.5 — Fonts.** Marcellus/Cormorant Garamond/Jost via `next/font/google`, CSS vars wired; real metadata set. **Build verified green** (Next 16 + Turbopack).
- ✅ **0.6 — Supabase client.** `lib/db/client.ts` (browser singleton) + `lib/db/server.ts` (server factory, no session) + `lib/env.ts` (zod-validated public env).
- ✅ **0.7 — Generated DB types.** `lib/db/types.ts` from live schema (7 tables incl. `primary_image_url`, `search`, `updated_at`); clients typed `<Database>`.
- ✅ **0.8 — Money utils.** `lib/utils/money.ts` (`formatPaise`, `paiseToRupees`, `discountPercent`) + `money.test.ts` — **4/4 pass via `bun test`**.
- ✅ **0.9 — App shell.** `Header` (maroon announcement bar + cream brand row, nav, cart stub) + `Footer` wired into `layout.tsx`; semantic landmarks. **Build green.** *(Reconcile visuals against storefront prototype — see note below.)*

## Phase 1 — Storefront (read-only)
Goal: browse catalog end-to-end from real Supabase data. No cart writes yet.

- ✅ **1.1 — Data access layer.** `lib/db/queries.ts`: `getCategories`, `getProducts(filters)`, `getProductBySlug`, `getFeaturedProducts`, `getApprovedReviews`. Typed, server-side; visible = all statuses except `Draft`; filters (category slug/material/price/search/featured) + sort + pagination; primary-image resolver with gradient (`bg`) fallback. **Query shapes smoke-tested against live Supabase** (embeds, inner-join filter, full-text search, detail).
- ✅ **1.1b — App shell visual reconciliation.** `Header` + `Footer` rebuilt to match the storefront prototype (decoded DSL + browser-rendered both): gradient announcement bar w/ offer+code (scrolls; only brand row + nav stick), left two-line brand lockup, centre search form, Account + Cart-with-badge, centred 7-item nav w/ gold underline hover; footer gains brand blurb + socials, Shop/Help columns, newsletter (UI-only), payments/GST strip. **Verified 1440 + 375** (flex-wrap responsive, no overflow). Closes the 0.9 follow-up note. *Announcement copy is static — wire to `setting.banner` in 1.3.*
- ✅ **1.2 — `ProductCard`.** `components/product/ProductCard.tsx` — prototype-matched: square image (real photo, else seed gradient + engraved motif), corner badge + sale flag, category eyebrow, serif name, ₹ price + struck MRP, ★ rating, Add stub. Whole card is a stretched-link to `/product/[slug]`; lift + shadow on hover, focus-visible on the name. **Rendered vs prototype at 4-across** (grid tiles, prices/strikes correct). *Note: Tailwind v4 didn't tile the `auto-fill` arbitrary grid — use explicit responsive `grid-cols-*` in 1.3/1.4. Add button wires to cart in Phase 2.*
- ✅ **1.3 — Home page.** `app/page.tsx` (async server component) composes `components/home/*`: `Hero`, `TrustStrip`, `CategoryTiles`, `ProductSection` (reused for bestsellers + new arrivals), `PromoBanner`, `StoryBlock` — all prototype-matched (decoded DSL + browser-rendered @1440 & @375). Wired to real Supabase data via new/extended DAL: `getCategoryTiles` (categories + visible-product count, from `category.hero_bg`), `getFeaturedProducts(8)`, new `getFreshProducts(8)` (`is_fresh` filter added to `getProducts`), and new `lib/db/settings.ts` `getStoreSettings` (typed `banner`/`homepage_promo` merged over defaults). Header announcement now data-driven from `setting.banner` (via `layout.tsx`), closing the 1.1b follow-up. **tsc clean; home renders 200 with live products; grids use explicit responsive `grid-cols-*`.** *Add buttons still stub → cart in Phase 2.*
- ✅ **1.4 — Category page.** `app/(storefront)/[category]/page.tsx` (bare-slug route per plan §4 → `/{slug}`): breadcrumb, title + product count, `ProductCard` grid, empty-collection state, and `generateMetadata` (name/description). Unknown slug → `notFound()` (verified **HTTP 404**); valid slug renders live products (verified 200 + browser @1440). New `getCategoryBySlug`; grid uses `getProducts({categorySlug})`. *Sort control + filter sidebar deferred to 1.6 (URL search-param state).* **Structure aligned to ARCHITECTURE_PLAN §4** — see ground-truth note above (`(storefront)` group + `components/storefront/*`).
- ✅ **1.5 — Product detail.** `app/(storefront)/product/[slug]/page.tsx` (server) composes `components/storefront/product/*`: `ProductGallery` (client — selectable thumbnails, gradient+motif fallback), `ProductBuyBox` (client — plating-tone selector, qty stepper, Add-to-Cart/WhatsApp **stubs** per Phase 2 scope), `ProductTabs` (client — Description/Details/Shipping, ARIA tablist), `ProductReviews` + shared `StarRating` (presentational). Prototype-matched (decoded DSL + browser-rendered @1440 & @375): breadcrumb (Home / Category / product), gallery+info split, price/struck-MRP/off badge, trust strip (free-ship threshold from `getStoreSettings`), reviews grid w/ empty state, "You may also love" rail via new `getRelatedProducts` (same category, current excluded). `generateMetadata` (name + blurb). Unknown slug → `notFound()` (**verified HTTP 404**); valid slug **200 + build green**. *Add/WhatsApp wire up in Phase 2; sort/filter still 1.6.*
- ✅ **1.6 — Search & filter.** All filter/sort state lives in **URL search params** (`sort`/`material`/`maxPrice`/`q`) via new `lib/listing.ts` (`parseListingParams`, price bounds). New **`/shop`** route = full catalog + search landing (header search form already POSTs `?q=` here); `/[category]` reworked to the same shared surface. Shared `components/storefront/listing/*`: `ProductListing` (server — title/count/sort header + two-column facets·grid + filtered empty state), `SortSelect` (client — Featured/price/rating), `FilterSidebar` (client — category nav w/ counts, material toggles, max-price slider committing on release, Clear all). DAL: new `getMaterials` (distinct); reuses `getCategoryTiles` for facet counts. Prototype-matched + **browser-verified @1440** (Kundan facet → 4 results); search `?q=jhumka` → "2 results"; **build green**. *Filters are single-select material + max-price for v1 (multi-select/min-price deferred, YAGNI).*
- ✅ **1.7 — Static/help pages.** Five routes under `app/(storefront)/`: `/shipping` (Shipping & Returns), `/care` (Care Guide), `/contact`, `/about`, `/faq`. Copy + content centralized in `lib/help-content.ts`; shared presentational primitives in `components/storefront/help/` — `HelpHeader` (maroon-gradient hero + breadcrumb), `HelpBlocks` (`HelpSection`/`IconCard`/`NumberCard`/`NumberedSteps`/`CheckList`/`RatesTable`), and `ContactForm` (client, UI-only — no endpoint yet). Shipping/Care/Contact **prototype-matched** (decoded DSL + browser-rendered); About/FAQ have no prototype so authored to brand voice in the same visual language (FAQ uses native `<details>` disclosures — a11y, no client JS). `generateMetadata`/`metadata` per page. `ROUTES.about`/`ROUTES.faq` added. **Build green; all 5 render 200; browser-verified @1440 + @375 (no overflow); no console errors.** *Contact/newsletter forms wire to a real handler in a later phase; footer nav still mirrors prototype (Shipping/Track/Care/Contact) — About & FAQ reachable by URL, surface in nav later if desired.*
- ✅ **1.8 — Responsive + a11y + visual pass.** Swept every storefront route (home, `/shop`, `/[category]`, `/product/[slug]`, all 5 help pages) with a scripted horizontal-overflow detector under CDP device emulation (Chrome window-min clamps `resize` to ~500px, so `emulate` is required for true 320). **Fixed a shared overflow bug:** two-column blocks used `min-w-[300px]`/`min-w-[280px]` on flex children — a hard floor that overflows at 320 (272px available) regardless of `flex-wrap`, expanding the mobile viewport by ~28px. Reworked to **stack full-width on mobile, restore the two-column `min-w`/`flex-1` layout at `md:`** in `Hero`, `ProductGallery`, product page info column, and `/contact` (wrapper + both columns). **Verified no horizontal overflow at 320/375/768/1024/1440** (docScrollW ≤ viewport on every page; two-column layouts confirmed `flex-direction: row` at ≥`md`). **a11y:** structural audit across home/product/contact/faq — single `h1` + ordered `h2`/`h3` hierarchy, all images/links/buttons have accessible names, all form inputs labelled (`aria-label`), landmarks (`header`/`nav`/`main`/`footer`) present, tabs expose `tablist`/`tab`/`tabpanel`, FAQ uses native `<details>`; keyboard focus preserved everywhere (native focus ring intact; the two `outline-none` inputs are compensated by wrapper `focus-within` borders, matching the `focus-visible:border-gold-400` pattern). **Contrast:** all core text passes AA comfortably (body 7.7–8.1, headings 13–17, footer 11.4, CTA 7.0); **fixed** gold-600 eyebrow labels (3.57 → AA-fail) by darkening the `--color-gold-600` token `#a87a1e → #94670f` (now 4.64:1 on cream-100), safe since gold-600 text only ever sits on light backgrounds. **tsc clean; build green (all 10 routes); no console errors.** *Known minor: input placeholders `#B79B7E` ≈ 2.62:1 — left as-is since every field has an `aria-label` (placeholder is a supplementary hint, not the accessible name) and darkening enough to pass would make it read like entered text.*

## Phase 2 — Cart & checkout (COD)
Goal: place a COD order. Introduces the first storefront **writes** (needs RLS insert policies).

- ✅ **2.1 — Cart store.** Split into a **pure domain core** + a **thin persisted store** (mirrors `lib/utils/money` — logic is testable without React). `lib/cart.ts`: `CartLine`/`CartLineInput` model (integer-paise **snapshots**, display-only — server recomputes at 2.5), immutable ops `addLine` (merges same product+variant, distinct variants stay separate lines, qty capped `MAX_LINE_QUANTITY=10`), `setLineQuantity` (clamps; ≤0 removes), `removeLine`, deterministic `cartLineId(productId, optionValue?)`, and derived selectors `cartCount`/`cartSubtotalPaise`/`cartMrpTotalPaise`/`cartSavingsPaise`. `stores/cart.ts` (`"use client"`): Zustand `useCartStore` wrapping those ops, `persist` → localStorage (`jr-cart` v1, `partialize` lines only) with a `hasHydrated` flag (set via `onRehydrateStorage`) so consumers render neutral until rehydrated (no SSR/CSR badge mismatch); actions `addItem`/`setItemQuantity`/`removeItem`/`clearCart`. **`bun test` 13/13 pass; tsc clean (cart code); build green (all 10 routes).** *UI wiring (buttons → store) is 2.2; the `bun:test` module tsc error on `*.test.ts` is pre-existing (Next build excludes tests).*
- ✅ **2.2 — Cart UI.** `/cart` route (`app/(storefront)/cart/page.tsx` — thin server shell supplying `freeShipThresholdPaise` from `getStoreSettings`) rendering client `components/storefront/cart/*`: `CartView` (container — store subscription + hydration gate + totals), `CartLineRow` (thumbnail w/ gradient+motif fallback, variant line, qty stepper, line total, Remove) and `CartSummary` (free-ship progress hint, Subtotal / You-save / Shipping / Total, gold checkout CTA, COD note). Coupon input intentionally deferred to 2.3. New pure `lib/shipping.ts` (`shippingPaise`/`amountToFreeShipPaise`/`qualifiesForFreeShipping` + `FLAT_SHIPPING_PAISE`; free = subtotal ≥ threshold, else ₹79 flat per Shipping page) with `lib/shipping.test.ts` **7/7**. **Add-to-cart wired live:** new `AddToCartButton` (client, reused by `ProductCard`) + `ProductBuyBox` (now takes product identity, adds selected plating variant as a distinct line) both push to the store with a brief "Added ✓"; header cart badge now live via new `CartBadge`. **Hydration fix:** replaced the store's `onRehydrateStorage`/`hasHydrated` flag (never flipped under Next's client hydration — cart stuck on "Loading…") with a `useCartHydrated()` hook built on zustand persist's own `onFinishHydration`/`hasHydrated()`, initialised `false` so SSR + first paint agree (no mismatch). Prototype-matched (decoded DSL + **browser-verified @1440**): filled cart (2 lines, badge 3, Subtotal ₹4,297 / You save −₹1,000 / Shipping FREE) and empty state; interactions exercised (increment → badge 4, Remove → 1 row, decrement-to-zero → empty state). **`bun test` 24/24; tsc clean; build green (11 routes incl. `/cart`).** `ROUTES.checkout` added for the summary CTA (page lands in 2.4). *Add button on cards adds the base product (no variant); variant selection is the product page.*
- ✅ **2.3 — Coupons.** New pure `lib/coupons.ts`: a hardcoded registry (`BRIDE20` = 20% off, matching the announcement banner — **no coupon table**, YAGNI until the store manages codes) + `validateCoupon(rawCode, subtotalPaise, now?)` (normalises case/whitespace, guards empty/unknown/**expired**/**below-min**) and `evaluateCoupon(coupon, …)`; discount is percent- or fixed-based, capped by `maxDiscountPaise` and clamped to `[0, subtotal]`. Tests `lib/coupons.test.ts` **9/9** (BRIDE20 20%, normalize, unknown/empty, min-guard, max-cap, expiry, never-exceeds-subtotal). Cart store gains persisted `couponCode` + `applyCoupon`/`clearCoupon` (holds the code only; validity + discount derived live from the subtotal, so removing items below a minimum drops the discount rather than leaving a phantom one). New client `CouponField` (input + Apply + applied-chip w/ Remove + inline error, matched to the prototype's summary coupon row) rendered as a slot in `CartSummary`, which now shows a **Discount** line and `Total = subtotal − discount + shipping`. **Client discount is display-only — 2.5 recomputes server-side and never trusts it.** Prototype-matched + **browser-verified @1440**: invalid code → "That code isn't valid."; `bride20` → chip "✓ BRIDE20 applied — 20% off", Discount −₹859.40, Total ₹3,437.60 (free-ship preserved); Remove → discount gone, Total ₹4,297, `couponCode` cleared from localStorage (apply/remove both persist). **`bun test` 33/33; tsc clean; build green (11 routes).**
- ✅ **2.4 — Checkout form.** `/checkout` route (`app/(storefront)/checkout/page.tsx` — thin server shell supplying `freeShipThresholdPaise`, breadcrumb + heading per prototype) rendering client `components/storefront/checkout/*`. New shared pure schema `lib/checkout/schema.ts` (`checkoutSchema` — fullName, phone `^[6-9]\d{9}$`, email, addressLine, city, state, pincode `^[1-9]\d{5}$`, `paymentMethod` enum `["cod"]`; exports `CheckoutFormValues`/`CHECKOUT_DEFAULTS`/`PAYMENT_METHODS`) used by **both** the client resolver and the server action, so the two can't drift — with `lib/checkout/schema.test.ts` **8/8**. `CheckoutView` (container — hydration gate, empty-cart guard → `/shop`, derives subtotal − live coupon discount + shipping), `CheckoutForm` (client — **React Hook Form** + `zodResolver(checkoutSchema)`, `mode:"onTouched"`, per-field inline errors, COD selected + "Card/UPI — coming soon" disabled, `<form>` wraps both columns so the summary's submit posts it), `CheckoutSummary` (presentational — line-item list w/ qty badges, Subtotal/Discount/Shipping/Total, gold "Place Order" submit). **Server validation:** new Server Action `submitCheckout` (`app/(storefront)/checkout/actions.ts`, `"use server"`) re-parses the SAME schema authoritatively and returns typed per-field errors (`CheckoutActionResult`); the client maps them via `setError`. Deps added: `react-hook-form` + `@hookform/resolvers`. **Order creation (writing `order`/`order_item`, server-recomputed totals) is deferred to 2.5** — the action stops at validation and the success branch shows an honest "details verified" notice (no fabricated order/redirect; that's 2.5/2.6). Prototype-matched (decoded DSL + **browser-verified @1440**): full form + summary render (Subtotal ₹4,297 / FREE / Total ₹4,297); empty submit → all 7 fields show inline errors + `aria-invalid`; valid submit → server round-trip OK → green verified banner w/ email, no errors; empty cart → prompt + "Start shopping". **`bun test` 41/41; tsc clean (checkout src); build green (12 routes incl. `/checkout`).** *COD is the only v1 tender (Razorpay deferred); totals stay display-only — 2.5 recomputes server-side and never trusts them.*
- ✅ **2.5 — Order creation.** New migration `supabase/migrations/0001_create_orders.sql` (applied to project `naolegptozpaiojozzcy`) adds the `order` + `order_item` tables (COD-ready: payment_method/status, awb, shiprocket_shipment_id nullable for later phases; integer-paise subtotal/discount/shipping/total; JR-YYMMDD-#### order numbers via `order_no_seq`) and a **SECURITY DEFINER `place_order(p_items, p_customer, p_coupon)` RPC** that is the **only** write path. **Chosen security model (user-approved): RPC, not anon RLS-insert** — because an RLS *insert* policy can't recompute prices, so the public anon key could otherwise post a faked total. Instead RLS is **enabled with zero policies (deny-all)** on both tables; only the definer function (owner-run, RLS-bypassing, `search_path=public`) writes. The function **recomputes every total from `product.price_paise`** (client never sends a price), mirrors the pure TS domain (`lib/cart` qty-clamp 1–10, `lib/coupons` BRIDE20 = 20% off, `lib/shipping` ₹79 flat below the store's free-ship threshold, shipping on pre-discount subtotal), rejects `Draft`/missing products + non-COD tenders, and returns `{order_no, subtotal/discount/shipping/total_paise}`. New pure `lib/checkout/order.ts` (`cartLinesToOrderItems` → price-free `{productId,tone,qty}` payload, `orderItemsSchema`, `toPlacedOrder` RPC-return narrowing) with `order.test.ts` **8/8**. Server Action `submitCheckout` rewritten: re-validates the form schema authoritatively, then calls the RPC via the anon client (granted execute); typed `{ok, order}` / `{ok:false, fieldErrors, formError}`. `CheckoutForm` sends items + live coupon + fields and lifts success via `onPlaced`; `CheckoutView` clears the cart and renders the new interim `OrderPlaced` panel (gated **above** the empty-cart check so it survives the clear). `lib/db/types.ts` regenerated (order/order_item rows + `place_order` fn). **Verified:** SQL unit-call (subtotal ₹2,096 → BRIDE20 −₹419.20, free ship, total ₹1,676.80) **and full browser E2E @1440** — seeded 2-line cart → filled form → Place Order → server round-trip → **real order `JR-260703-1001`** persisted with server-recomputed subtotal/total **1159700** (client sent no prices), tone snapshots (Gold/null) intact, cart badge 3→0, "Order placed" panel with ref + amount. Test order deleted + sequence reset after each check. Security advisors reviewed: `rls_enabled_no_policy` (INFO) + `anon/authenticated_security_definer_function_executable` (WARN) are **expected-by-design** (the deny-all lockdown + the intended public checkout RPC). **`bun test` 49/49; tsc clean; build green (12 routes).** *Confirmation page `/order/[orderNo]` + redirect is 2.6; a coupon-rule change must be mirrored in both `lib/coupons.ts` and the SQL function until a coupon table lands (admin Phase 3).*
- ✅ **2.6 — Confirmation page.** New route `app/(storefront)/order/[orderNo]/page.tsx` (server) renders `components/storefront/order/OrderConfirmation.tsx` — **mirrored from the prototype's `isConfirm` screen** (decoded DSL: gold 88px ✓, Cormorant "Thank you for your order!", "Your order {orderNo} is confirmed · sent to {email}", card with Order number / Amount / Estimated delivery **4–7 days**, Continue Shopping); copy adjusted for COD ("Amount **payable**" + "keep the amount ready" instead of the prototype's Razorpay "Amount paid"). **URL keyed on the order number** (matches ARCHITECTURE_PLAN §4's `[orderNo]`), but a bare sequential number is guessable, so **the order number now carries a 4-char random base36 suffix** (`JR-YYMMDD-####-XXXX`, ~1.7M combos/sequence) — it's both the customer reference and an unguessable bearer capability, so the URL is human-friendly *and* non-enumerable (chosen over the uuid on user feedback). Migrations (applied): `0002_order_confirmation.sql` added the read-only confirmation RPC; `0003_order_no_token.sql` regenerates `place_order`'s number with the suffix (return drops `order_id`) and re-keys **`get_order_confirmation(p_order_no text)`** (SECURITY DEFINER, STABLE, `search_path=public`, granted anon/authenticated) — returns only non-line-item fields (order_no, status, payment_method, customer_email, total_paise, created_at); the `order`/`order_item` tables stay RLS-sealed (no read policy). New server helper `lib/db/orders.ts` (`getOrderConfirmation(orderNo)` → `toOrderConfirmation`, null → `notFound()`); `lib/checkout/order.ts` adds `OrderConfirmation`/`toOrderConfirmation` (`PlacedOrder` keys on `orderNo`; order.test.ts **10/10**). `CheckoutView` **clears the cart + redirects** to `ROUTES.order(orderNo)` via `useRouter` (an `isRedirecting` gate above the empty-cart check prevents an empty-prompt flash); the interim `OrderPlaced.tsx` from 2.5 is deleted (superseded). Page metadata is `robots: noindex`; a malformed/unknown number 404s (order-no-regex guard + null-result guard). `lib/db/types.ts` regenerated. **Verified E2E** (checkout submit → redirect to `/order/JR-260703-1001-00N6`; confirmation renders order no + email + Amount payable **₹3,299**, cart badge →0; **bogus order number → 404**) — the confirmation page was verified against the **production build** (`next start`) because the shared dev server on :3000 hadn't hot-reloaded the `[id]→[orderNo]` route rename. Test orders deleted + sequence reset after each check. **`bun test` 51/51; tsc clean; build green (13 routes incl. `/order/[orderNo]`).** *Full order tracking/lookup (Track Order) is later; WhatsApp enquiry is 2.7.*
- ✅ **2.7 — WhatsApp enquiry.** New pure `lib/whatsapp.ts` — `wa.me` link builders from the single `STORE_INFO.whatsapp` number + `STORE_INFO.name` (so number/brand never drift): `whatsappUrl(message)` (URL-encodes), `productEnquiryMessage/Url({ name, tone?, url? })` ("Hi RJ Jewellers, I'm interested in the {name} ({tone} plating).\n{product URL}", tone appended only when a plating is chosen, and the absolute product page link on its own line — the page derives it from the request host via `headers()` — so the store can open the exact product), and `cartEnquiryMessage/Url(lines)` (lists `• {qty}× {name} ({tone}) — {unit}` per line + Subtotal). Tests `lib/whatsapp.test.ts` **7/7** (encoding + round-trip, tone on/off, cart listing + subtotal). Wired: **`ProductBuyBox`** "Enquire on WhatsApp" stub → real `<a target="_blank" rel="noopener noreferrer">` whose href recomputes from the live plating-tone selection; **`CartSummary`** gains a `whatsappHref` prop + a matching green enquiry link below the checkout CTA, built by `CartView` from the cart lines (the cart context the prototype lacked — added per the plan). All links open the store number with a prefilled chat; no PII/URL params. **Verified in-browser @1440:** product page → `wa.me/919999999999?text=Hi RJ Jewellers, I'm interested in the Bridal Combo … (Gold plating).`; cart page → multi-line cart enquiry with `Subtotal: ₹11,597`; both `target=_blank rel=noopener noreferrer`. **`bun test` 58/58; tsc clean; build green (13 routes).** *Contact-page WhatsApp (plain chat, no prefill) already wired via `STORE_INFO` in the store-info task. **Phase 2 (Cart & COD checkout) complete.***
- ✅ **2.8 — Customer auth & authorization** (0577880, f23f530, 9e216ce). **Answers the open question: checkout is sign-in only (no guest orders — user decision).** Sign-in methods: email+password, passwordless email-OTP (6-digit), and Google OAuth (code shipped; **provider must be enabled in the Supabase dashboard** — OAuth client ID/secret + Site/Redirect URLs — before the button works). **DB (migration 0004, applied + verified):** `customer_profile` (own-row RLS), `order.user_id` + owner-SELECT policies on order/order_item (writes stay RPC-only), `place_order` raises `AUTH_REQUIRED` for anon (verified: anon → `42501 permission denied`, authenticated-without-uid → `AUTH_REQUIRED`) and stamps `user_id`; execute revoked from anon. **Sessions:** `@supabase/ssr` cookie sessions — `lib/db/server.ts` is async + cookie-aware (RLS runs as the customer), `lib/db/client.ts` is the cookie-based browser client, `proxy.ts` refreshes sessions merged with the CSP response. **UI (no prototype — designed in the storefront checkout language):** `/sign-in` (password + OTP toggle + Google + `?next=`), `/sign-up` (check-your-email state; confirmation ON), `/forgot-password` → `/account/reset-password`, `/auth/callback` (PKCE exchange + `safeNext()` open-redirect guard, unit-tested), `/account` (profile: name/phone/default address → `customer_profile`), `/account/orders` (RLS-scoped history w/ status chips; footer **Track Order** now points here), session-aware header (Sign In ↔ Account). **Checkout:** gate redirects to sign-in and returns; form prefills from profile + account email; `submitCheckout` re-checks the session and saves the checkout contact as the profile on success. **Verified:** `bun test` 80/80; tsc clean; build green (7 new routes, all `ƒ`); sign-in page CSP-clean in Chrome; HTTP gates confirmed (`/account`, `/checkout` → 307 to sign-in). Visual baselines regenerated (+sign-in surface, 20 total, stable across runs). **E2E:** signed-out-redirect test passes; the full journey signs in with the seeded `chetangkajjidoni+e2e@gmail.com` account (creds in gitignored `.env.local`) and is **pending one manual click on its confirmation email** (self-skips until then). *Old orders have `user_id = null` (pre-auth history is invisible in My Orders — acceptable; only test orders existed).*
- ✅ **2.8b — Passwordless email flow fixed** (af1a406). The default Supabase "Magic Link" template sends a **link** (no `{{ .Token }}` code) that redirected to the dashboard Site URL and dead-ended. Redesign: `/auth/callback` handles **`token_hash`+`type`** (verified server-side — any browser, allowlist-independent; `recovery` → reset-password), **`code`** (PKCE/OAuth), and **Supabase error bounces** (`otp_expired` → friendly banner); `signInWithOtp` passes `emailRedirectTo`; the sent-state offers **both** the emailed link and 6-digit-code entry. **Verified live:** E2E journey 3/3 (sign-in → checkout → confirmation → My Orders); real magic-link `token_hash` → `/auth/callback` → 307 `/account` **with session cookies**; consumed/garbage/expired → `/sign-in?error=expired`. Test orders cleaned, seq reset. **Recommended one-time dashboard step (Auth → Email Templates):** point links at the callback and include the code — Magic Link: `<p>Sign in: <a href="{{ .SiteURL }}/auth/callback?token_hash={{ .TokenHash }}&type=magiclink&next=/account">Sign in to JR Jewellers</a></p><p>Or enter this code: {{ .Token }}</p>`; Confirm signup: same with `type=signup`; Reset password: same with `type=recovery` (no `next` needed — callback routes recovery itself). Until then, default-template links work same-browser via the PKCE branch once `http://localhost:3000/auth/callback` (+ prod URL later) is in Auth → URL Configuration → Redirect URLs.

- ✅ **2.8c — Branded auth email templates.** The dashboard's auth emails were bare one-line `<p>` snippets (2.8b's minimal fix). New `supabase/templates/` — four email-client-safe HTML templates (table layout, inline styles, 560px card) aligned with the storefront UI: maroon `#2A1115` brand band + gold wordmark/descriptor, cream `#FBF6EE` background, `#FFFDF8` card with `#E7D9C2` hairlines, gold-gradient CTA (2px radius, uppercase tracking), Cormorant Garamond/Jost with Georgia + system-sans fallbacks (email clients rarely load web fonts). Files: `magic-link.html` (CTA + one-time `{{ .Token }}` code box), `confirm-signup.html`, `reset-password.html`, `email-change.html` (`{{ .Email }}` → `{{ .NewEmail }}`) — all linking through `/auth/callback?token_hash={{ .TokenHash }}&type=…` per 2.8b, each with plain-URL fallback + expiry/ignore notes. `apply.sh` PATCHes subjects + bodies to the project via the Management API (`SUPABASE_ACCESS_TOKEN=sbp_... ./supabase/templates/apply.sh`); `README.md` documents the dashboard-paste alternative + the Site URL gotcha. **Browser-verified all four @700 + magic-link @375 (fluid, no overflow).** *Not yet applied to the Supabase project — needs a personal access token (or a dashboard paste). Order-confirmation email remains unimplemented (needs an email provider — later phase).* **Follow-up fix:** the project's Email OTP Length is 8, but `otpVerifySchema` hardcoded `^\d{6}$` — the emailed code was rejected client-side before reaching Supabase. Regex now `^\d{6,10}$` (any allowed dashboard setting works; Supabase stays the authoritative check) and the sign-in copy/label say "one-time code" instead of "6-digit". **bun test 80/80; tsc clean.**

## Phase 3 — Admin console
*Spec source: `refereces/JR Admin Dashboard.html` — decoded the `__bundler/template` DSL AND browser-rendered every view (2026-07-03). 13 views + 3 modals + an auth screen. Mirror the prototype exactly: dark-maroon sidebar (wordmark, 10 nav items with count badges, Store Admin footer card), cream content area, topbar (page title + subtitle, global "Search orders, products…", notification bell with badge), softer 8–12px radii (vs the storefront's 2–4px). All writes go through **service-role key server-side only** (new `SUPABASE_SECRET_KEY` env + `lib/db/admin.ts` client, `import "server-only"`) — the anon key keeps zero write policies.*

- ✅ **3.0 Admin foundation.** New `(admin)` route group (sibling to `(storefront)`, so it renders in the root layout **without** the customer Header/Footer). `app/(admin)/admin/layout.tsx` fetches nav counts + wraps `AdminShell`; `metadata.robots` = noindex for the whole console. Chrome mirrors the decoded + browser-rendered prototype: dark-maroon `#2A0A12` `AdminSidebar` (`STORE_INFO` gold wordmark + "Admin Console", 10 nav items with line-art icons + active gold text/left-bar/tint via `usePathname`, live count badges, Store Admin footer card) and a sticky blurred `AdminTopbar` (Cormorant page title/subtitle derived from the route, global search + notification bell — **UI-only** for now). All 10 views are real routes (`/admin` … `/admin/settings`) rendering an honest `AdminPlaceholder` (title + "Phase 3.x" chip + what's coming). Single-source registries: `ROUTES.admin*`, `lib/admin/nav.ts` (`ADMIN_NAV` + `ADMIN_PAGE_META`, both feeding sidebar **and** topbar). **New service-role client** `lib/db/admin.ts` (`import "server-only"`, reads `SUPABASE_SECRET_KEY`, RLS-bypassing — the Phase-3 read/write path; anon key keeps zero write policies) + `lib/db/admin-metrics.ts` `getAdminNavCounts` (pending orders `status='Pending'` + pending reviews `status='pending'`; **messages = 0 until the `contact_message` table lands in 3.8**; degrades to zeros if the key is unset or a query fails, so the chrome always renders). Responsive: static sidebar at `lg`, off-canvas drawer + burger + backdrop below. **tsc clean (only the pre-existing `bun:test` errors); build green — all 11 admin routes `ƒ`; browser-verified @1440 (active state + full chrome) and @390 (drawer opens/closes over a backdrop, no overflow); a11y tree clean (aside/nav/banner/main landmarks, single h1, `aria-current`).** *Follow-ups: (1) **set `SUPABASE_SECRET_KEY` in `.env.local`** (Supabase service-role/secret key) to light up the badges + power all 3.2+ admin data — until then counts read 0; (2) routes are **unauthenticated until 3.1** adds the admin gate — the foundation exposes only aggregate counts (no PII) and real-data views land after the gate; (3) sidebar rail-collapse (mini-icon mode) deferred — full sidebar + mobile drawer only; (4) wordmark uses `STORE_INFO` ("RJ", not the prototype's "JR").*
- ✅ **3.1 Auth & admin gate.** Admin access = `app_metadata.role='admin'` — a JWT claim only the service role can set (tamper-proof), and the runtime gate reads it from the **cookie session** so it needs **no** secret key. `lib/admin/roles.ts` `isAdmin` (pure, Edge-safe) + `lib/admin/auth.ts` `requireAdmin` (server-only: `getCurrentUser` + redirect). **Route split:** `/admin/*` divided into a gated `(console)` group (chrome + the 10 views, moved via `git mv`) and an ungated `(auth)` group (`/admin/sign-in`, `/admin/forgot-password`, `/admin/reset-password`) so the sign-in page isn't itself behind the gate. **Two layers:** `proxy.ts` coarse-redirects non-admins off the console (307, preserving `?next=`, skipping the `(auth)` paths); the `(console)` layout re-checks authoritatively with `requireAdmin` before any data is fetched or chrome renders. **UI** reuses the storefront auth primitives (`AuthField`/`AuthSubmit`/`AuthError`/`AuthLink`) under a wordmark-topped `AdminAuthShell`: email+password sign-in (`signInWithPassword` → client re-checks `isAdmin` and signs a valid non-admin straight back out with "no admin access"), forgot-password → "check your email", reset-password; `/auth/callback` recovery is now admin-aware (honours a `next` of the admin reset path). **Sign out** = icon button in the sidebar footer, which now shows the signed-in admin's real email (threaded layout→shell→sidebar). Migration `0005_admin_role.sql` (**applied**) grants the owner `chetangkajjidoni@gmail.com`. **Verified against a production `next start`:** unauthenticated `/admin`, `/admin/orders`, `/admin/settings` → **307 → `/admin/sign-in?next=…`**; `(auth)` pages → 200; signed in as a granted admin → console renders with the real email + working sign-out → back to sign-in; a11y names intact. **tsc clean; build green — 32 routes (incl. the 3 admin-auth `ƒ`).** *For a working login now, the E2E account `chetangkajjidoni+e2e@gmail.com` is currently granted admin — revoke with `update auth.users set raw_app_meta_data = raw_app_meta_data - 'role' where email = 'chetangkajjidoni+e2e@gmail.com';` once the owner account exists (re-run the migration then). `SUPABASE_SECRET_KEY` still only needed for the nav-count badges + 3.2+ data, not the gate. Storefront customer auth (2.8) unchanged and separate.*
- ✅ **3.2 Dashboard** — real KPIs + widgets computed live from `order`/`order_item`/`product`, no fake data. `lib/db/admin-dashboard.ts` `getDashboardData()` runs the reads in parallel through the admin's **own cookie session** (see below), buckets by **IST** (the store TZ, so "today"/the 7-day chart match the operator's day, not UTC), and **degrades to an empty dashboard on any error** so the chrome always renders. Presentational `DashboardView` (`components/admin/dashboard/`) mirrors the decoded + browser-rendered prototype: 4 KPI cards (Orders Today +Δ% vs yesterday, Revenue Today +Δ%, Pending Orders → "Needs processing", Low/Out of Stock → "Restock now"; Δ shows "No baseline yesterday" when yesterday had none, not a divide-by-zero ∞); Revenue last-7-days bar chart (gold bars, maroon peak, compact ₹k/L/Cr labels); Recent Orders mini-table (5 latest — order no, customer, date, total, `statusChip`, "View all →"); Low Stock Alerts (name + SKU + "N left"/"Out of stock" chips, ≤5 threshold); Top Sellers This Month (units + revenue, current calendar month, cancelled orders excluded). Shared `lib/admin/order-status.ts` `statusChip()` (the 6 fulfilment states → label+colour) so a status looks identical across the console. **Key-free admin reads:** migration `0006_admin_read_policies.sql` (**applied**) adds an `is_admin()` SQL fn (JWT `app_metadata.role='admin'`) + additive `select` RLS on `order`/`order_item`/`review` for the `authenticated` role — an admin's cookie session reads them directly, the anon/publishable key still can't, and writes stay RPC-only. Refactored `getAdminNavCounts` off the service-role client onto the same cookie session, so **the badges + dashboard now work without `SUPABASE_SECRET_KEY`** (`lib/db/admin.ts` service-role client kept, reserved for future admin **writes** in 3.3+). **tsc clean; build green — all 32 routes incl. `/admin`.** *Order-derived cards read empty until orders exist (the store's orders were cleaned); low-stock reads `product.stock` live.*
- ✅ **3.3 Orders** — the full fulfilment queue, prototype-matched. **List** (`components/admin/orders/OrdersView.tsx`, client): filter-tab pills with live per-status counts (All / Pending / Confirmed / Packed / Shipped / Delivered / Cancelled — the real `order.status` enum; the spec's "Paid" is the separate `payment_status`, so the fulfilment tab is **Confirmed**), table (order no, customer + city/state, date, item count, total, payment method, status chip), **URL-driven** `?status=`/`?page=` filter + 6-per-page pagination (shareable, server-rendered `<Link>`s). **Drawer** (`OrderDrawer.tsx`, client, slide-in over a backdrop): 5-step fulfilment stepper (Pending→Confirmed→Packed→Shipped→Delivered; a "cancelled" notice replaces it for cancelled orders), customer contact + full address, item lines (name · tone ×qty) + subtotal/discount/shipping/total, single primary advance action (`Mark as {next}` — one step at a time via `advanceLabel`/`nextStatus` in `lib/admin/order-status.ts`) + Cancel. **Shiprocket AWB card is a deliberate stub** — buttons render *disabled* with a "coming in a later phase" hint (real integration stays deferred with Razorpay), no fake success. **Data** (`lib/db/admin-orders.ts` `listAdminOrders`): parallel per-status head counts + one filtered/paginated rows query carrying full detail + line items (so the drawer opens with no round-trip), IST dates, degrades to an empty page on error; reads via the admin cookie session (0006 RLS). **Write** = admin-only server action `setOrderStatus` (`orders/actions.ts`: `requireAdmin` gate → `admin_set_order_status` RPC → `revalidatePath`) over migration `0007_admin_order_status.sql` — a SECURITY DEFINER RPC gated on `is_admin()` that enforces forward-by-one transitions + cancel-from-any-non-terminal server-side (tables stay RLS-sealed for writes; **no service-role key needed**). On a successful change the drawer **stays open and reflects the new status in place** (holds a local `AdminOrderRow` snapshot, not a `page.rows` lookup, so it survives revalidation even when the order leaves the active filter); only the backdrop or × closes it, and a failed change shows a friendly error (fixed in `992f111` — previously it snapped shut on every advance). **tsc clean; build green — `/admin/orders` compiles.** *Follow-ups: (1) migration `0007` **applied + verified** (owner ran it; probed the live RPC — returns `NOT_ADMIN` for a non-admin caller, confirming the function exists and the `is_admin()` gate fires; an admin session now advances/cancels successfully). Minor hardening available but not required: `revoke execute on function admin_set_order_status(uuid,text) from anon;` (anon can enter the function but `is_admin()` blocks any write, so no exposure). If the remote types are ever regenerated, the hand-added `admin_set_order_status` signature in `lib/db/types.ts` will be superseded. (2) No automated browser/E2E pass yet — verified by build + type-check + the live RPC probe; a screenshot pass is still worth doing. (3) Pagination renders all page numbers (fine at store scale; add windowing/ellipsis if order volume grows).*
- 🟡 **3.4 Products CRUD (core done; CSV import deferred).** **List** (`components/admin/products/ProductsView.tsx`, client): name/SKU search + category + status dropdowns, all **URL-driven** (`?search`/`?category`/`?status`/`?page`, shareable/server-rendered), table (thumb from `primary_image_url` or `PLACEHOLDER_GRADIENT`, name + SKU, category, price with strike-through MRP, stock colour-coded, derived status chip Active/Low stock/Out of stock/Draft), 8-per-page pagination. **Add/Edit modal** (`ProductModal.tsx`): name, SKU, category, Price + optional Sale price (₹ → integer paise; sale = charged `price_paise`, Price = strike-through `mrp_paise`), stock, status (Active/Draft), image URL, badge, material, short tagline (`blurb`), description (`desc_long`), plating/stones/care details, shipping note, Featured/Fresh toggles — every field maps to a real `product` column. **Data** `lib/db/admin-products.ts` (`listAdminProducts` search/category/status/paginate + `getAdminCategories`) reads via the admin cookie session (product is publicly readable, so Drafts are visible); each row carries all editable fields so the edit modal prefills with no round-trip. **Write** = `upsertProduct` server action (`products/actions.ts`: `requireAdmin` → `admin_upsert_product` RPC → `revalidatePath`) over migration `0008_admin_products.sql` — SECURITY DEFINER, `is_admin()`-gated, insert (auto-unique slug from name) or update (slug preserved); maps SKU unique-violation → friendly error; tables stay RLS-sealed for writes, **no service-role key**. Client-safe `lib/admin/product-status.ts` holds the filter/badge/status constants + chip helpers (keeps the server-only data module out of the browser bundle). **tsc clean; build green — `/admin/products` compiles.** **Modal redesigned to match `JR Admin Dashboard.html` exactly** (decoded DSL + browser-rendered): full-width name/SKU, a **Designs & images** card (multi-image grid, each tile a click-to-upload target with Design name + Primary toggle, "N of 6 designs"), Category/Material/Badge row, Price/Sale/Stock row, **Plating options** chips, Description, **Product details** card (finish/stones/care), Shipping info. Status/Featured/Fresh aren't in the prototype modal, so they're **carried through** from the row on edit (defaults Active/false on create) rather than shown. **Media backing** — migration `0009_admin_product_media.sql` adds `gallery jsonb` (ordered `{url,name,primary}`) + `plating_options text[]` and re-emits `admin_upsert_product` to persist them; `primary_image_url` stays the denormalised storefront thumbnail. **Image upload** — real Supabase Storage via `uploadProductImage` server action + migration `0010_product_images_storage.sql` (public `product-images` bucket; SELECT public, INSERT/UPDATE/DELETE `is_admin()`-gated), so uploads use the admin cookie session (no service key, anon can't write). **Follow-ups: (1) apply migrations `0008` + `0009` + `0010`** (owner) — create/edit + upload need all three; reads need 0009 (the list SELECT references `gallery`/`plating_options`). (2) **CSV import modal + `admin_bulk_insert_products` RPC still to build.** (3) per-row analytics link lands with 3.10. (4) no live browser/E2E pass yet (admin login required) — verified by build + type-check.*
- ✅ **3.5 Categories** — prototype-matched card grid + modal CRUD. **List** (`components/admin/categories/CategoriesView.tsx`, client): "New Category" button over a responsive `auto-fill/minmax(260px)` card grid — each card a gold-tint icon tile (`hero_bg` or `PLACEHOLDER_GRADIENT`), Cormorant name, live product-count subtitle (`categoryCountLabel`), and an edit pencil; empty-state prompt when there are none. **Add/Edit modal** (`CategoryModal.tsx`): 440px card with name + description (the only fields in the prototype modal; `slug`/`sort_order`/`hero_bg` are managed server-side, not exposed), inline error, and — on edit — a **two-step Delete** (first click arms "Confirm delete?" so a destructive click can't fire by accident). **Data** `lib/db/admin-categories.ts` `listAdminCategories` reads `category` (all rows) via the admin cookie session and tallies `product.category_id` in JS for per-collection counts; the `AdminCategoryRow` type + `categoryCountLabel` live in the **client-safe** `lib/admin/category.ts` (mirrors `product-status.ts`) so the client views don't drag the server-only module into the browser bundle. **Write** = `upsertCategory`/`deleteCategory` server actions (`categories/actions.ts`: `requireAdmin` → RPC → `revalidatePath`) over migration `0011_admin_categories.sql` (**applied + verified**): SECURITY DEFINER, `is_admin()`-gated `admin_upsert_category` (insert auto-slugs from name + appends `sort_order`; update preserves slug/order) and `admin_delete_category`. **Delete re-home strategy = block** (TASKS gave block-or-reassign): deleting a category that still holds products raises `CATEGORY_HAS_PRODUCTS` → friendly "move or remove them first" (a silent reassign would move a product's URL/breadcrumb out from under the customer; reassign UI deferred as YAGNI). `lib/db/types.ts` hand-extended with both RPC signatures. **Verified:** the `is_admin()` gate blocks a non-admin caller (0 rows inserted); an admin-JWT SQL simulation ran the full path green — create (slug `qa-temp-cat`, sort_order appended), update (name changed, slug preserved), delete-with-products **blocked**, clean delete succeeded, category count back to baseline (nothing persisted). **tsc clean; build green (33 routes; `/admin/categories` `ƒ`); bun test 80/80.** *No live browser pass yet (admin login required) — verified by build + type-check + the admin-JWT data-layer round-trip, matching the 3.3/3.4 bar.*
- ✅ **3.6 Coupons** — **lands the `coupon` table and kills the documented BRIDE20 drift risk** (migration `0012_admin_coupons.sql`, **applied + verified**). The hard-coded `BRIDE20 = 20%` rule that lived in **both** `lib/coupons.ts` and `place_order` is gone — both now read the table. **Schema:** `coupon` (`code` unique, `kind` percent/fixed/free_shipping, `value` [0–100 % · paise · 0], `min_subtotal_paise`, `max_discount_paise`, `usage_limit` nullable, `usage_count`, `expires_at`, `is_active`); BRIDE20 seeded so nothing regresses. **RLS:** public read of *usable* coupons only (active, unexpired, under cap) so the storefront preview is table-driven; admin reads all; writes RPC-only (no service key). **`place_order` (same signature/return):** the coupon block is a single **guarded `UPDATE … RETURNING`** that atomically checks (active/unexpired/under-cap/meets-min) **and** consumes one use — row-locked, so concurrent orders can't exceed `usage_limit`; a non-matching code silently yields no discount. Free-shipping codes zero the shipping line (not the discount). **Storefront:** new `lib/db/coupons.ts` `getActiveCoupons` feeds the cart/checkout page shells; `validateCoupon(code, subtotal, coupons)` is now pure + registry-driven (breaking sig — all 3 client callers updated); `CouponField`/`CartView`/`CheckoutView` thread the coupons through and honour `freeShipping`. Client stays display-only. **Admin** (prototype-matched — decoded DSL + browser-rendered @1440): `CouponsView` (Create Coupon button + table: code, discount, min order, usage `N used`/`N / limit`, expiry, inline **active toggle** via `toggleCoupon`) + `CouponModal` (460px — code, type+value, min order + date expiry; value hidden for free-shipping). **Create + toggle only, matching the prototype** (no edit/delete affordance in the design). `lib/admin/coupon.ts` holds the client-safe `AdminCouponRow` + cell-label helpers; `lib/db/admin-coupons.ts` reads all rows via the admin cookie session; `coupons/actions.ts` (`upsertCoupon`/`toggleCoupon`, `requireAdmin` → `admin_upsert_coupon`/`admin_toggle_coupon` RPCs, rupees→paise parse). `lib/db/types.ts` hand-extended with the `coupon` table + both RPC signatures. **Verified:** non-admin write blocked (`NOT_ADMIN`), an admin-JWT SQL round-trip ran create → toggle off/on → below-min guard (no consume) → discount math (BRIDE20: ₹1000→₹200, ₹2500→₹500) all green in a rolled-back tx (nothing persisted; BRIDE20 usage still 0). **`bun test` 84/84; tsc clean (only pre-existing `bun:test` errors); build green (34 routes; `/admin/coupons` `ƒ`).** *Follow-ups: (1) no live browser/E2E pass yet (admin login required) — matches the 3.3–3.5 bar. (2) usage-limit / max-discount aren't in the prototype's create modal, so they're settable only in the DB for now (columns + list display support them; add fields if the store needs them). (3) active coupon codes are exposed to the client for the sync preview — fine for advertised promo codes; add a server-action single-code validator if truly secret codes are ever needed. (4) edit/delete deferred (prototype offers neither).*
- ✅ **3.6b Coupons — close the gaps (advisor review).** Five improvements the 3.6 self-check missed, from the Fable review, all shipped (migration `0013_coupon_gaps.sql`, **applied + verified**). **(1) Silently-dropped coupon surfaced:** `place_order` now returns `coupon_dropped` (true when a code was entered but no row matched — expired/exhausted/below-min between preview and placement). It threads through `toPlacedOrder` (`PlacedOrder.couponDropped`, defaults false) → `CheckoutView` redirect `?coupon=dropped` → order page `searchParams` → an amber "your coupon was no longer valid, so it wasn't applied" notice on `OrderConfirmation`. Charged totals unchanged (the RPC already recomputes authoritatively — this is pure UX feedback, confirmed no overcharge). **(2) Usage-limit + max-discount in the Create modal:** `CouponModal` gained "Usage limit" (placeholder *Unlimited*) and "Max discount (₹)" (percent-only, disabled otherwise) fields; `CouponInput`/`upsertCoupon` gained `usageLimit`/`maxDiscount` with a `toCountOrNull` parse, wired into the RPC payload (previously hardcoded null). **(3) Coupon use released on cancel:** `admin_set_order_status` now reads `coupon_code` and, on the Cancel branch, `update coupon set usage_count = greatest(0, usage_count - 1)` (cancel is terminal → fires at most once). **(4) Expiry timezone:** modal expiry stored as `T18:29:59.000Z` (end-of-day IST) instead of `T23:59:59.000Z` UTC, so an India coupon no longer stays valid ~5.5h into the next IST day. **(5) Percent > 100 rejected:** guarded in both the modal `onSave` and the `upsertCoupon` action ("A percentage discount can't exceed 100%.") instead of the RPC silently clamping. **Verified** in rolled-back SQL probes: invalid code → `coupon_dropped: true`, valid code → `false` + 10% discount applied; a coupon order consumed one use (`usage_count 0→1`) and cancelling via the admin RPC **released** it (`1→0`); `is_admin()` gate still blocks non-admin callers. **`bun test` 86/86 (84 + 2 new `couponDropped` cases); tsc clean (only pre-existing `bun:test` errors); build green (34 routes).** *Follow-ups: no live browser/E2E pass yet (admin login required) — matches the 3.3–3.6 bar; the max-discount/usage-limit fields now close two of 3.6's four follow-ups.*
- ✅ **3.7 Reviews moderation** — prototype-matched moderation console over the existing `review` table (no new table needed; schema + read policies already shipped in 0006). **UI** (`components/admin/reviews/ReviewsView.tsx`, client): Pending / Approved / All filter pills with live counts over a responsive `auto-fill` card grid — each card shows the product (uppercase gold), a status pill (amber/green/red), star rating, title, body, and author · date; a **pending** card carries ✓ Approve / × Reject actions (prototype gates actions on pending only). Lands on the **Pending** tab (the moderation queue); tab filtering is client-side (no pagination in the prototype). **Data** `lib/db/admin-reviews.ts` `listAdminReviews` reads every review via the admin cookie session (0006 `review_admin_read`) newest-first and resolves each product name from a companion `product` read (JS map, mirroring `admin-categories.ts` rather than an embedded join); returns `{ rows, counts }`. Client-safe `AdminReviewRow` + `reviewStatusChip`/`reviewStars`/`reviewDateLabel` + the `ReviewFilter`/`ReviewCounts` types live in `lib/admin/review.ts` (mirrors `lib/admin/coupon.ts`). **Write** = `setReviewStatus` server action (`reviews/actions.ts`: `requireAdmin` → RPC → `revalidatePath`) over migration `0014_admin_reviews.sql` (**applied + verified**): SECURITY DEFINER, `is_admin()`-gated `admin_set_review_status` that validates the status against `pending|approved|rejected` and 404s a missing row. `lib/db/types.ts` hand-extended with the RPC signature. The sidebar **Reviews badge already reads the pending count** (`admin-metrics.ts`), so it now reflects the queue live. **Storefront renders approved-only — verified:** the public RLS policy is `status = 'approved'` and `getApprovedReviews` additionally filters `status='approved'`, so a rejected/pending review never reaches a product page. **Verified:** non-admin write blocked (`NOT_ADMIN`); an admin-JWT SQL round-trip flipped a seeded pending review pending→approved→rejected; an invalid status raised `INVALID_STATUS`; all in rolled-back txns. Seeded 3 realistic **pending** reviews (real products) so the queue is demonstrable (Pending 3 / Approved 7 / All 10). **`bun test` 86/86; tsc clean (only pre-existing `bun:test` errors); build green (34 routes; `/admin/reviews` `ƒ`).** *Follow-ups: (1) no live browser/E2E pass yet (admin login required) — matches the 3.3–3.6 bar. (2) prototype-faithful: actions show on pending cards only, so a mis-approved review can't be reverted from the UI (would need to show the opposite action on approved/rejected cards) — deferred as the prototype offers no such affordance. (3) no customer review-submission path yet (out of scope for moderation; reviews are seeded/imported).*
- ✅ **3.8 Messages** — new `contact_message` table + the storefront Contact form now actually submits (was UI-only). **Schema** (migration `0015_contact_messages.sql`, **applied + verified**): `contact_message` (`ticket_no` unique `TK-YYMMDD-###`, `subject` nullable, `body`, `name`, `email` + `phone` [both required — see the 0016 follow-up below], `status` New/In Progress/Resolved default New, `created_at`) + a `contact_message_no_seq` counter. **RLS-sealed for writes** (admin-read-all policy; no public read, no public insert). **Public submit** = `submit_contact_message` SECURITY DEFINER RPC **granted to anon** that validates required fields, clamps lengths, mints the ticket number atomically (IST date via `Asia/Kolkata`) and inserts as New — the only write path, mirroring `place_order`. **Storefront** `ContactForm` posts through the `submitContactMessage` server action (shared `lib/contact/schema.ts` zod, used client + server so they can't drift) with a **honeypot** enforced server-side like checkout; on success it shows the gold-check confirmation with the **ticket reference**, and field/form errors surface inline. **Admin** (prototype-matched — decoded DSL): `MessagesView` — All / New / In Progress / Resolved pills with live counts over a card grid (ticket no · status pill · subject · sender · message · date), context actions **Start** (New→In Progress), **✓ Resolve** (→Resolved), **Reopen** (Resolved→In Progress) via the `admin_set_message_status` RPC (`is_admin()`-gated, validates status, 404s a missing row). Client-safe `AdminMessageRow` + `messageStatusChip`/`messageDateLabel` in `lib/admin/message.ts`; `lib/db/admin-messages.ts` reads all tickets (admin RLS) + derives counts; `messages/actions.ts` = `setMessageStatus`. `lib/db/types.ts` hand-extended with the table + both RPC signatures. The sidebar **Messages badge** now counts New tickets (`admin-metrics.ts`, previously hardcoded 0). **Verified** (rolled-back txns): anon `submit_contact_message` minted `TK-260704-001` and the row was **invisible to anon** (RLS sealed); admin flow New→In Progress→Resolved→(reopen)In Progress all correct; non-admin `admin_set_message_status` blocked (`NOT_ADMIN`). Seeded 3 tickets (one per status) so every tab is demonstrable. **`bun test` 86/86; tsc clean (only pre-existing `bun:test` errors); build green (36 routes; `/admin/messages` + `/contact` both `ƒ`).** *Follow-ups: (1) no live browser/E2E pass yet (admin login required) — matches the 3.3–3.7 bar. (2) honeypot only (per the task, "like checkout"); a public anon-callable submit is a spam surface — rate-limiting is 3.9's requirement and a sensible add here too. (3) no email notification on new ticket (out of scope).*
  - **3.8a follow-up — collect email + phone, add contact actions** (migration `0016_contact_message_email_phone.sql`, **applied + verified**). Split the single freeform `contact` field into required **`email`** + **`phone`** columns (both NOT NULL; existing rows back-filled from the old field before the flip) so the admin has both lines to reach the customer. The shared `contactMessageSchema` now validates a real email and a 10-digit Indian mobile (`^[6-9]\d{9}$`, reused from checkout); `submit_contact_message` takes/stores both; the storefront form shows separate **Email** + **Phone** inputs (`type=email`/`tel`, autocomplete). The admin `MessagesView` shows both details and gives one-click **Call** (`tel:`), **WhatsApp** (`wa.me/91…`, new tab) and **Mail** (`mailto:`) action buttons per ticket (each renders only when its detail is present, so a legacy row without a phone degrades cleanly). Verified: anon submit with both fields stored `email`+`phone` and minted the next ticket; the 3 seed rows updated to carry both. **`bun test` 86/86; tsc clean; build green.** *This is an explicit deviation from the prototype's single contact field, at the user's request.*
- ✅ **3.9 Subscribers** — new `subscriber` table + the footer "Stay in touch" form now actually saves (was UI-only). **Schema** (migration `0017_subscribers.sql`, **applied + verified**): `subscriber` (`email`, `source` footer/checkout/popup default footer — only `footer` used in v1, checkout/popup pre-allowed for the deferred surfaces, `created_at`) + a **case-insensitive unique index** `on lower(email)` so one row per address regardless of casing. **RLS-sealed for writes** (admin-read-all policy; no public read, no public insert). **Public subscribe** = `subscribe_email(p_email, p_source)` SECURITY DEFINER RPC **granted to anon** that lowercases + shape-checks the address, clamps length, and `insert … on conflict (lower(email)) do nothing`, returning `{status: subscribed|already}` so a repeat sign-up is idempotent — the only write path, mirroring `place_order`/`submit_contact_message`. **Storefront** `NewsletterForm` (extracted client component; `Footer` stays a server component) posts through the `subscribe` server action (shared `lib/subscribe/schema.ts` zod, client + server) which is **rate-limited** (`lib/rate-limit.ts` — new in-memory fixed-window limiter, 5 attempts / 10 min per client IP via `x-forwarded-for`; best-effort per-instance, DB unique index is the real boundary) **and honeypot-guarded** like checkout/contact; on success the form is replaced by a gold confirmation. **Admin** (prototype-matched — decoded DSL + browser spec): `SubscribersView` — three KPI cards (**Total subscribers**, **Joined this week**, **Top source** `Footer · N`) over a "Mailing list" card with an email **search** (client filter), **Copy emails** (clipboard, visible rows), **Export CSV** (client blob download, CSV-escaped) and a table (gradient avatar initial · email · joined · source pill · **× remove**) with an empty state. Remove goes through `admin_remove_subscriber(p_id)` (`is_admin()`-gated, 404s a missing row) via `removeSubscriber` action. Client-safe `AdminSubscriberRow` + `subscriberSourceChip`/`subscriberInitial`/`subscriberDateLabel` in `lib/admin/subscriber.ts`; `lib/db/admin-subscribers.ts` reads all rows (admin RLS) + derives the KPIs; `subscribers/actions.ts` = `removeSubscriber`. `lib/db/types.ts` hand-extended with the table + both RPC signatures. (Subscribers nav has no badge, so no `admin-metrics` change.) **Verified** (rolled-back txns): anon `subscribe_email` → `subscribed`; uppercase re-subscribe → `already` (case-insensitive de-dupe); anon **can't read** the table (`0`, RLS sealed); invalid email → `INVALID_EMAIL`; non-admin `admin_remove_subscriber` → `NOT_ADMIN`; admin read → `1`, admin remove → row id then `0`. Seeded 3 footer sign-ups so the KPIs/list/export are demonstrable. **`bun test` 86/86; tsc clean (only pre-existing `bun:test` errors); build green (32 routes; `/admin/subscribers` `ƒ`).** *Follow-ups: (1) no live browser/E2E pass yet — matches the 3.3–3.8 bar (the footer form is publicly testable, unlike the admin views, so a Playwright happy-path is a cheap add later). (2) rate limiter is in-memory/per-instance — swap for Redis/Upstash behind the same `checkRateLimit` signature if this scales to multiple instances. (3) checkout opt-in & the popup capture surface stay deferred (schema already allows their `source` values).*
- ✅ **3.10 Analytics** — Product Analytics, computed from real `order_item` history (the prototype's fake sparkline data is gone). **No migration** — pure read over the existing 0006 `order`/`order_item` admin-read RLS (no new tables/RPCs, no `types.ts` change). **Server** `lib/db/admin-analytics.ts` `getProductAnalytics()`: reads the last-6-months, **non-cancelled** orders + their line items through the admin cookie session, buckets them by **IST month** (Asia/Kolkata, matching the order-no date rule), and folds into per-product `units6mo` / `revenuePaise6mo` / a 6-point monthly history / a **recent-3-vs-prior-3 trend %** (null when there's no prior baseline, rendered "—"). Every product appears — zero-sale items carry zeros so the sort still covers the whole catalogue. KPIs: **Units sold · 6 mo**, **Revenue · 6 mo**, **Best seller**, **Low / out of stock** (reuses `productDisplayChip`). **Client** `AnalyticsView` (prototype-matched — decoded DSL + browser spec): a **list** view (4 KPI cards + a "Performance by product" table with a **Sort by** dropdown — Top selling / Revenue / Lowest stock / Highest price — real per-row spark bars, price/stock/units/revenue/trend, colour-coded stock + trend) and a **detail** view (back link, header card with status pill + `sku · category` + **Edit product** link to `/admin/products`, six stat cards, and a units-per-month bar chart). List ↔ detail is client `selectedId` state, so the whole dataset loads once server-side — no route change. Client-safe types + `sortAnalytics`/`trendLabel`/`trendColor`/`sparkBars` in `lib/admin/analytics.ts`; money via the shared `formatPaise`. **Verified** against SQL ground truth (rolled into the read path): 8 orders / 12 products → **Units 13**, **Revenue ₹43,287**, best seller **"Kundan Rani Haar Bridal Set"** (8 units), low/out **0** — matches the module's KPIs exactly; all current-month sales so trend correctly reads "—" and the latest history bar populates. **`bun test` 86/86; tsc clean (only pre-existing `bun:test` errors); build green (32 routes; `/admin/analytics` `ƒ`).** *Follow-ups: (1) no live browser/E2E pass yet — matches the 3.3–3.9 admin bar (login-gated). (2) "Edit product" lands on the catalogue list (editing is modal-based there; no per-product route exists) rather than deep-linking the modal. (3) with real 6-month spread the trend/history light up automatically — the seed data just happens to sit in one month.*
- ✅ **3.11 Settings** — four prototype cards over the single `setting` row, all saved through one write path. **Schema** (migration `0018_admin_settings.sql`, **applied + verified**): `admin_update_settings(p_payload jsonb)` SECURITY DEFINER, `is_admin()`-gated, **partial-safe** (each column only overwritten when its key is present, via `p_payload ? 'key'`), clamps lengths + non-negative money, and updates `store_name` / `support_email` / `phone` / `gstin` / `free_ship_threshold_paise` / `flat_rate_paise` / `cod_enabled` / `banner` / `homepage_promo`. `razorpay_live` is intentionally **not writable** (the toggle renders disabled/"Coming soon"). Same RPC-only model as every other admin write; no service key. **Shipping is now the source of truth**: `place_order` re-created to read `flat_rate_paise` from `setting` (was a hardcoded `7900`) alongside the free-ship threshold it already read — **all subtotal/coupon/total maths byte-for-byte unchanged**; `lib/shipping.ts` `shippingPaise` gained a `flatRatePaise` param (defaults to the fallback constant) threaded from `getStoreSettings` through the cart + checkout views, so the storefront delivery line follows the setting too. **Admin** (prototype-matched — decoded DSL + browser spec): `SettingsView` — **Store Information** (name / support email / phone / GSTIN), **Shipping & Payments** (free-ship ₹ + flat ₹ edited in whole rupees ↔ paise at the boundary, COD switch, disabled Razorpay switch), **Announcement Banner** (enable + **live preview** + msg1/msg2/offer label/highlight/promo code) and **Homepage Promo Block** (enable + **live preview** + eyebrow/headline/button/code/note), with a single **Save Changes** that persists everything via the `updateStoreSettings` action → RPC → `revalidatePath("/", "layout")` so the storefront banner/promo/shipping update **live**. Shared `settingsFormSchema` + rupee↔paise mappers in `lib/admin/settings.ts` (client + server, type-only import from the server-only settings module so it stays client-safe); `getStoreSettings` extended to expose support email/phone/gstin/flat rate/COD/Razorpay; `lib/db/types.ts` hand-extended with the RPC. **Verified** (rolled-back txns): `place_order` shipping followed a probe `flat_rate_paise` of **5500** (not the old 7900); `admin_update_settings` as admin persisted name/flat(4200)/threshold(88800)/banner and **left `razorpay_live` untouched**; non-admin call → `NOT_ADMIN`; the live `setting` row is intact (₹999 free-ship / ₹79 flat / COD on). **`bun test` 86/86; tsc clean (only pre-existing `bun:test` errors); build green (32 routes; `/admin/settings` `ƒ`).** *Follow-ups: (1) the COD toggle persists `cod_enabled` but checkout isn't gated on it yet — with Razorpay deferred, COD is the only method, so enforcing "off" would break checkout; wire it when payments land. (2) no live browser/E2E pass — matches the admin bar. (3) Razorpay Live Mode stays read-only until the payments phase.*

## Phase 4 — Production readiness (from `STOREFRONT_SHORTFALLS.md`, audit 2026-07-05)
Goal: clear the launch blockers found in the storefront audit (code review + browser pass @1440/@375). Verdict there: **not production-ready** until 4.1–4.6 land. Item numbers in *(parens)* reference the audit doc.

### 4A — Launch blockers
- ✅ **4.1 — Truth fixes in the UI.** Footer payment strip changed from the false "UPI · Cards · Netbanking · COD · Razorpay Secured" to **"Cash on Delivery · Secure Checkout"** (`Footer.tsx`) — matches the COD-only reality *(1.4)*. Confirmation page no longer claims an email was sent — reworded to **"Keep this order number handy — we'll use {email} to reach you about it"** (`OrderConfirmation.tsx`), honest until real transactional email lands in 4.6 *(1.3)*. **tsc clean (only pre-existing `bun:test` errors); browser-verified** on home (footer strip) and `/order/[orderNo]` (confirmation copy, real seeded order `JR-260704-1006-60TS`).
- ✅ **4.2 — Legal pages.** Three new routes — `/privacy`, `/terms`, `/refund-policy` — closing the compliance gap (India's Consumer Protection (E-Commerce) Rules 2020: return/refund terms, seller details, grievance contact). New `lib/legal-content.ts` (mirrors `help-content.ts`: `PRIVACY_SECTIONS`/`TERMS_SECTIONS`/`REFUND_SECTIONS`, `LEGAL_LAST_UPDATED`) + `components/storefront/help/LegalBlocks.tsx` (`LegalSection`/`LegalUpdatedNote` — denser prose primitives than `HelpBlocks`' icon cards). Each page reuses `HelpHeader` for the hero and reads contact details from `STORE_INFO`; Terms includes a Grievance Officer clause, Refund Policy cross-links the existing friendly `/shipping` page. `ROUTES.privacy`/`terms`/`refundPolicy` added; footer bottom bar gains a Privacy/Terms/Refund-Policy legal nav next to the copyright line. **tsc clean (only pre-existing `bun:test` errors); browser-verified** all three pages @1440 (renders match brand typography/palette) and the footer wrap @375 (no overflow) *(1.5)*.
- ✅ **4.3 — Branded 404 / error / loading pages.** Nine new files close the gap. **`(storefront)/not-found.tsx`** + **`(storefront)/error.tsx`** (client, `reset()` + "Back to Home") render inside the storefront layout so Header/Footer still wrap them — gold "404"/"!" badge, on-brand copy, CTA buttons, matching `OrderConfirmation`'s visual language. **Root `app/not-found.tsx`** covers genuinely unmatched top-level paths that never reach a route group (no Header/Footer available there — self-contained with the `STORE_INFO` wordmark instead). **Root `app/global-error.tsx`** (client, imports `./globals.css` directly since it replaces the root layout entirely per the Next.js convention) is the last-resort fallback if the root layout itself throws. **Loading skeletons:** new `ListingSkeleton` (mirrors `ProductListing`'s breadcrumb/header/sidebar/grid shape) wired via `loading.tsx` in `shop/` and `[category]/`; new `ProductDetailSkeleton` (gallery + buy-box shape) wired via `loading.tsx` in `product/[slug]/` — all `animate-pulse` placeholders, no data fetching. **tsc clean (only pre-existing `bun:test` errors); browser-verified:** `/product/does-not-exist` and a bare-slug 404 (`/foo`) both render the branded storefront 404 with full chrome (replacing the old black panel); a multi-segment unmatched path (`/foo/bar/baz`) correctly falls through to the standalone root 404 *(1.6)*.
- ✅ **4.4 — Stock enforcement** (migration `0019_stock_enforcement.sql`, **applied + verified** against `naolegptozpaiojozzcy`, user-confirmed before applying). `place_order` now locks each product row `for update` (serializes concurrent orders against the same product), rejects a line with `OUT_OF_STOCK: {name}` when requested qty exceeds available stock, and decrements `product.stock` in the same transaction as the order write. `admin_set_order_status` restores stock on Cancel (mirrors the 3.6b coupon-use release). **Incidental fix (found while rewriting the function):** `place_order`'s `auth.uid()` gate + `user_id` stamping — added in `0004_customer_auth.sql` — had been silently dropped when `0012_admin_coupons.sql` re-created the function for the coupon-table rewrite, and the gap carried unnoticed through `0013`/`0018`. Confirmed live: the 3 most recent orders had `user_id = null`. `execute` was never re-granted to `anon` (0004's revoke still stood), so this was never an anonymous-write hole — but authenticated customers' orders since 0012 weren't tagged to their account (masked in testing because the test account is admin-granted and a separate RLS policy lets admins read every order, not just their own). Restored both fixes in the same migration. **Verified via rolled-back SQL probes:** anon call → permission denied (unchanged); authenticated oversell (qty 999 vs stock 7) → rejected; authenticated in-stock order → succeeded with `user_id` correctly stamped; admin cancel on a real Pending order → stock restored (18→19, rolled back). `order_no_seq` reset after the probe (sequences aren't transactional). **Storefront UI:** `AddToCartButton` gained a `disabled` prop; `ProductCard` shows a grey "Out of Stock" badge (suppresses the normal badge/sale-flag) + disabled "Sold Out" Add button when `stock <= 0`; `ProductBuyBox` clamps the qty stepper to `min(10, stock)` and disables Add with an "Out of Stock" label. **tsc clean; browser-verified** on both the product page and the `/shop` grid (temporarily zeroed a live product's stock, confirmed the disabled/badge states, restored it immediately) *(1.2)*.
- ⬜ **4.5 — Real product photography.** Every product/category renders a seed gradient; gallery shows "PRODUCT IMAGE — YOUR PHOTO HERE". Content task: shoot/source photos, upload via the admin product modal (Storage pipeline works; `primary_image_url` auto-fills) *(1.1)*.
- ✅ **4.6 — Transactional email.** Order-confirmation email on successful checkout via **Resend**, built to degrade gracefully: with no `RESEND_API_KEY` (dev/CI/today's prod) everything is a no-op and checkout is untouched. **`lib/email/order-confirmation.ts`** — pure, unit-tested template builder (subject/html/text): brand-palette inline-style table layout (email clients can't load webfonts — Georgia/system stacks), order number, COD total via `formatPaise`, delivery address, "keep ₹X ready" notice, View-your-order button (`SITE_URL` + `ROUTES.order`), WhatsApp/support footer from `STORE_INFO`; **every customer-entered field HTML-escaped** (`escapeHtml`). **`lib/email/send.ts`** — server-only sender: plain authenticated POST to the Resend REST API (one endpoint doesn't warrant the SDK), 10s timeout, logs-never-throws; `queueOrderConfirmationEmail` schedules the send via Next's **`after()`** so the customer never waits on the mail provider (detached-send fallback outside request scope); `isEmailEnabled()` exposes config state. **Checkout:** `submitCheckout` queues the email after a successful `place_order` (post profile-upsert, pre-return). **4.1 copy stopgap closed:** the confirmation page passes `emailSent={isEmailEnabled()}` — copy reads "A confirmation email is on its way to {email}" when configured, keeps the honest "we'll use {email} to reach you" fallback otherwise. **Env:** optional `RESEND_API_KEY` + `EMAIL_FROM` documented in `.env.example` (default `onboarding@resend.dev` sender delivers only to the Resend account owner — set `EMAIL_FROM` after domain verification). **Testing:** 5 new template unit tests (order no/total/address/link in both bodies, XSS escaping, blank-name fallback); checkout tests assert the email queues once on success with the right to/orderNo/total and never on failure. **`bun test` 91/91; build green; `tsc --noEmit` now fully clean** — added dev-dep `@types/bun`, which also erased the "pre-existing `bun:test` errors" caveat carried since Phase 2. *Follow-ups: (1) needs a real `RESEND_API_KEY` + verified domain at deploy (4.13) — until then behavior is unchanged; (2) shipped/delivered status notices + admin new-order alert (the deliberate 4.6 deferrals) can reuse `sendEmail` + a template each; (3) no live send exercised yet — verify deliverability (SPF/DKIM) once the domain is verified.*

### 4B — Mobile funnel (browser-verified defects)
- ✅ **4.7 — Mobile nav drawer.** New `components/storefront/layout/MobileNavDrawer.tsx` (client): a hamburger trigger `md:hidden`, and an off-canvas drawer (`PRIMARY_NAV` links + account link) mirroring `AdminSidebar`'s fixed-backdrop + slide-in-panel pattern in storefront chrome (cream bg, sharp radii, maroon/gold). `Header.tsx`'s desktop `<nav>` strip is now `hidden md:block`. **Bug found + fixed during browser verification:** the drawer's `position: fixed` was initially nested inside the sticky header's `backdrop-blur-md` wrapper — a `backdrop-filter` on an ancestor makes that ancestor the containing block for `fixed` descendants (CSS spec), so the drawer was pinned to the ~124px header bar instead of the viewport (confirmed via `getBoundingClientRect()`: `top:54, bottom:178`). Fixed by rendering the backdrop+drawer through a `createPortal` to `document.body` (guarded by an `isMounted` state to avoid an SSR `document` reference), leaving only the hamburger trigger inline. **tsc clean; browser-verified @375**: hero now renders in the first viewport (nav strip no longer wraps into 4 rows), drawer opens full-height with all 7 links + Sign In, closes via ×/backdrop; desktop @1440 unchanged (full nav strip, no hamburger) *(2.1)*.
- ✅ **4.8 — Mobile filter drawer on `/shop`.** `FilterSidebar` gained an `isMobileOpen` state and a "Filters" disclosure button (`lg:hidden`, shows "· Active" when a facet is applied, caret rotates on toggle); the category/material/price/clear-all block is now `hidden` by default on mobile and only `flex` when expanded (always `flex` at `lg:`, untouched). No fixed positioning involved this time — an inline collapse, not an off-canvas drawer, so the `backdrop-filter` containing-block bug from 4.7 doesn't apply here. **tsc clean; browser-verified @375**: first product card now renders in the first viewport collapsed (vs. y≈1471px before); tapping "Filters" expands the full facet stack in place, pushing the grid down; desktop @1440 unchanged *(2.2)*.
- ✅ **4.9 — Card badge collision.** `resize_page`'s window floors at ~500px, which had masked this at "375" in earlier passes — switched to the `emulate` tool's `viewport` override (real CDP device metrics) to reproduce at a true 375px width: `getBoundingClientRect()` showed the badge/flag overlapping by ~29px. Fixed in `ProductCard.tsx` with a `flagPositionClass` (`hasBadge ? "left-3 top-10 sm:left-auto sm:right-3 sm:top-3" : "right-3 top-3"`) — below `sm` (the 2-col grid range, ~150px cards) the sale flag stacks under the badge instead of sharing the top row; `sm:` and up (3-col grid, wider cards) keeps the original corner layout unchanged. **tsc clean; browser-verified**: no overlap/truncation at a true 375px viewport (home rail + `/shop` grid), desktop @1440 pixel-identical to before *(2.3)*.

### 4C — Pre-deploy hygiene
- ✅ **4.10 — Rate-limit checkout + contact.** Extended the existing `checkRateLimit`/`clientRateKey` utility (previously newsletter-only) to `checkout/actions.ts` and `contact/actions.ts`. Checkout throttles per authenticated `user.id` (checkout is sign-in only, so a precise per-account key beats per-IP — 5 attempts/10min); contact throttles per client IP like the newsletter (5/10min). Both return the existing `{ ok: false, formError }` shape on rejection — no new UI states needed. Still the same in-memory, per-instance limiter (real fix — an edge store — is deploy-time, tracked under 4.13). **tsc clean**; not separately browser-tested (pure reuse of the already-exercised newsletter rate-limit path, applied via the same two-line call site) *(3.2)*.
- ⬜ **4.11 — Observability.** Error tracking (e.g. Sentry) + privacy-friendly analytics + an uptime check — production incidents are currently invisible *(3.6)*.
- ⬜ **4.12 — Staging DB for E2E.** Tests write real orders to the live Supabase project (hand-cleaned); use a branch/staging project so CI can run *(3.7)*.
- 🔄 **4.13 — Deploy steps.** *In progress — deploying the Docker image via **Dokploy** (self-hosted, Docker Compose) rather than Vercel.*
  - ✅ **Compose env resolution fixed** (`7174f2d`): the deploy failed with `.../code/.env.local: no such file` — `docker-compose.yml` hard-required `.env.local`, which is gitignored so it's absent from Dokploy's clone. Both `.env`/`.env.local` env_files are now `required: false` (a missing file can't fail the build), and every runtime var is passed via `environment:` interpolation so secrets come from **Dokploy's Environment panel**, not a committed file. Validated with `docker compose config` → VALID.
  - ⬜ **Set env in the Dokploy panel** — build-time (Next inlines these): `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`; runtime: `NEXT_PUBLIC_SITE_URL` (real domain), plus optional `SUPABASE_SECRET_KEY` / `RESEND_API_KEY` / `EMAIL_FROM` / `ADMIN_ALERT_EMAIL`. (Open Q: confirm Dokploy exports the panel into the *build* step, not just runtime.)
  - ⬜ **Remaining:** domain/HTTPS on the Dokploy host; enable Google OAuth in the Supabase dashboard; run `supabase/templates/apply.sh`; update Supabase Site URL/redirect allowlist off localhost; set a verified Resend domain (4.6/5.2 email goes live only then); shared rate-limit store *(L5)*; re-run E2E/visual against the deployed host *(§4)*.

### 4D — Post-launch fast follows
- ✅ **4.14 — Customer order detail + cancel** (migration `0020_customer_cancel_order.sql`, **applied + verified** against `naolegptozpaiojozzcy`, user-confirmed before applying). New `/account/orders/[orderNo]` page: `getMyOrderDetail` (`lib/db/orders.ts`) reads via the existing "customer reads own orders"/"customer reads own order items" RLS policies (0004) — no new RPC needed for reads. New `OrderDetail` component reuses `buildStepper`/`statusChip` from `lib/admin/order-status.ts` restyled in storefront chrome: status stepper, items, delivery address, totals. My Orders rows now link here via new `ROUTES.accountOrder` (was `ROUTES.order`, the post-checkout confirmation). **Cancel:** new `CancelOrderButton` (two-step confirm, not a native `confirm()`) + `cancelMyOrder` action call the new `customer_cancel_order` SECURITY DEFINER RPC, which mirrors `admin_set_order_status`'s Cancel branch (release coupon use, restore stock) but is scoped to `order_no + user_id = auth.uid()` and Pending-only. `lib/db/types.ts` extended with the new RPC's `Args`/`Returns`. **Verified via rolled-back SQL probes:** anon → `AUTH_REQUIRED`; authenticated on a Confirmed order → `ORDER_NOT_CANCELLABLE: Confirmed`; authenticated as a non-owner → `ORDER_NOT_FOUND` (doesn't leak that the order exists under someone else); authenticated owner on their own Pending order → succeeds, status flips to Cancelled, stock restored (7→8, 18→19 matching each line's qty) — then rolled back and confirmed unchanged. **Then a real (non-rolled-back) browser click-through** signed in as the seeded E2E account (its stored password had drifted from `.env.local`'s value, so it was reset via `crypt()`/`gen_salt('bf')` against `auth.encrypted_password` and `.env.local` updated to match): clicked Cancel on a real Pending order (`JR-260703-1001-OKGU`) → confirm row → "Yes, cancel" → page refreshed showing the Cancelled chip + "This order was cancelled" notice, Cancel button gone. Confirmed order still shows stepper-at-Confirmed + no Cancel button. **tsc clean; browser-verified** *(2.5)*.
- ✅ **4.15 — Review submission** (migration `0021_customer_reviews.sql`, **applied + verified** against `naolegptozpaiojozzcy`, user-confirmed before applying). `review` predates customer auth (no `user_id`) — migration added a nullable `user_id` (existing seed rows unaffected) + a partial unique index `(product_id, user_id) where user_id is not null` (one review per product per customer) + a new `submit_review` SECURITY DEFINER RPC mirroring `place_order`/`customer_cancel_order`'s pattern (auth.uid() gate, validates rating 1–5 + body ≥10 chars server-side as a backstop, rejects a second review on the same product, inserts `status='pending'`). New `lib/review/schema.ts` (shared zod bounds), `app/(storefront)/product/[slug]/actions.ts` (`submitReview`), `components/storefront/product/StarRatingInput.tsx` (clickable 1–5 picker, write counterpart to the read-only `StarRating`), `components/storefront/product/ReviewForm.tsx` (sign-in prompt when signed out; form + pending-confirmation state when signed in) wired into `ProductReviews`/the product page (prefills name from `customer_profile`). `lib/db/types.ts` extended (`review.user_id`, `submit_review` Args/Returns). **Verified via rolled-back SQL probes:** anon → `AUTH_REQUIRED`; rating outside 1–5 → `INVALID_RATING`; body <10 chars → `INVALID_INPUT`; valid submission → `{status:"pending"}`; a second submission for the same product by the same account → `ALREADY_REVIEWED`. **Then a real browser submission** signed in as the seeded E2E account: 5-star pick + review text → "Thanks for your review!" confirmation → row confirmed in the DB with `status='pending'`. **Side effect (expected, not a bug):** a pre-existing trigger (`review_sync_rating`, recalculates `product.rating`/`review_count` from `status='approved'` rows on every `review` write) fired on this insert and corrected a stale seed value (the header showed "4.8 · 126 reviews" but the actual review list only ever had 3 rows) down to the true "4.7 · 3" — confirmed the trigger only counts `approved` rows, so the new `pending` review didn't skew the average. **tsc clean; browser-verified** *(2.6)*.
  - **Follow-up (user-flagged, same session):** the initial version let any signed-in customer review any product, purchased or not — no "Verified Purchase" gate. **Migration `0022_review_requires_purchase.sql`, applied + verified**: `submit_review` now requires a `Delivered` order containing the product (checked between the product-exists and rating checks; function body only, same signature). New `hasDeliveredPurchase` (`lib/db/orders.ts`, same RLS-scoped pattern as `listMyOrders`) lets the page know eligibility before rendering anything. **Verified via rolled-back SQL probes:** unpurchased product → `PURCHASE_REQUIRED`; purchased-but-already-reviewed product → falls through the purchase check silently and correctly hits `ALREADY_REVIEWED` next, proving the checks compose correctly.
  - **UX follow-up (user-requested):** dropped the sign-in prompt and "Only customers who've received this product can write a review" notice entirely — `ReviewForm` now takes a single `hasPurchased` prop (implies signed-in, since the page only checks purchase status for a logged-in user) and renders `null` when false. The "Write a Review" section simply isn't there for anyone who hasn't received the product; no message, no partial state. **Browser-verified both states**: Kundan Rani Haar (this account's real Delivered order) shows the form; Polki Choker Necklace Set (never purchased) shows nothing at all after the review list.
- ✅ **4.16 — SEO surface.** All four Next metadata surfaces added. New `lib/site-url.ts` (`SITE_URL`, optional `NEXT_PUBLIC_SITE_URL` env, falls back to `localhost:3000`) backs `app/sitemap.ts` (static marketing/legal pages + every category + every storefront-visible product, excludes `Draft`) and `app/robots.ts` (disallows `/admin`, `/account`, `/cart`, `/checkout`, `/sign-in`, `/sign-up`, `/forgot-password`, `/auth/`; points at `/sitemap.xml`). Root layout (`app/layout.tsx`) gained `metadataBase` + default `openGraph`/`twitter` fields. New `app/opengraph-image.tsx` (edge `next/og` `ImageResponse`, branded maroon/gold card — no real product photography exists yet, see 4.5) serves as the site-wide default OG image. New `lib/seo.ts` (`buildOrganizationJsonLd`, `buildProductJsonLd`) + `components/seo/JsonLd.tsx` (a `nonce`'d `<script type="application/ld+json">` — required because the strict CSP's `script-src 'nonce-…' 'strict-dynamic'` would otherwise drop a hand-written inline script; new `lib/csp-nonce.ts` reads the existing `x-nonce` request header `proxy.ts` already sets for Next's own bootstrap scripts) render Organization JSON-LD site-wide and Product JSON-LD (name/description/image/SKU/Offer with INR price + stock-based availability/AggregateRating when reviewed) on each product page. Product page `generateMetadata` also sets per-product `openGraph` title/description + image. **Bug found + fixed during verification:** Next.js replaces a parent segment's `openGraph` metadata object wholesale (no deep-merge) — an early version that only set `openGraph` when a real product photo existed silently dropped the inherited default OG image for every photo-less product (i.e. nearly the whole catalog). Fixed by always setting `openGraph` with an explicit `images` array, falling back to `${SITE_URL}/opengraph-image` when no product photo exists. **tsc clean; verified:** `curl /robots.txt` and `/sitemap.xml` (30 URLs: static + categories + all storefront-visible products) both 200 with correct content-types; `/opengraph-image` 200 `image/png`; both JSON-LD blocks confirmed rendering with a nonce and correct data in the browser (Organization's `sameAs` correctly omits the still-null Facebook/Instagram links per `STORE_INFO`; Product's `aggregateRating` matches the live 4.7/3 figure from 4.15's trigger fix); `og:image`/`og:title`/`og:description` all correct on both a product with no photo (falls back to the branded default image) and the site root *(2.4)*.
- ✅ **4.17 — Listing pagination.** New `getProductsPage` (`lib/db/queries.ts`) — a dedicated paginated query (kept separate from `getProducts`, which stays untouched for its 5 other unpaginated callers): counts first with a head-only query, clamps the requested page into `[1, pageCount]`, *then* ranges the main data query — never asking PostgREST for an out-of-range offset. Shares `getProducts`' row-mapper (extracted to `mapProductRows`). `lib/listing.ts` gained `PRODUCTS_PAGE_SIZE` (24), a `page` URL param, and `buildListingHref` (preserves every other active filter param when building a page link). `FilterSidebar`/`SortSelect` now strip `page` on every filter/sort change so a stale page number never survives a result-set change. New `Pager` in `ProductListing.tsx` (Prev/numbered/Next, storefront-styled mirror of the admin console's pager) renders when `pageCount > 1`. `/shop` and `/{category}` both switched from `getProducts` to `getProductsPage`. **Two real bugs found + fixed during verification:** (1) an out-of-range page (e.g. a stale `?page=99` after a filter shrank the results) made PostgREST raise "Requested range not satisfiable" as an unhandled 500 — fixed by clamping the page *before* the ranged query runs, and propagating the actually-served (possibly-clamped) page back to the UI so the pager's active-page/Prev-Next state never disagrees with what's rendered; (2) the count-only query's `.select("id")` had no `category` embed, so `.eq("category.slug", …)` — needed for the count query on category pages — silently had nothing to attach to and threw; fixed by embedding `category:category!inner(slug)` in the count select, matching the main query's join. **tsc clean; browser-verified**: pagination UI (temporarily set `PRODUCTS_PAGE_SIZE=3` to exercise it, since the live catalog is only 12 products) shows correct page counts/active state/Prev-Next disabling, page links preserve other params, filter/sort changes reset to page 1, `?page=99` clamps correctly (to page 1 at the real page size, to the true last page at the reduced size) with no crash and a consistent pager, and `/bridal-sets` (the category-filtered path) renders correctly post-fix *(3.1)*.
- ✅ **4.18 — Performance/caching pass.** Routes stay dynamic (the nonce CSP is non-negotiable) but the DB reads underneath are now cached cross-request, so a warm request renders without touching Supabase. **Read layer:** every public catalog read (`lib/db/queries.ts` — categories, category-by-slug, products, products-page, materials, category tiles, product-by-slug, approved reviews; `lib/db/settings.ts` — `getStoreSettings`) is wrapped in `unstable_cache` with tags from new `lib/db/cache.ts` (`products`/`categories`/`reviews`/`settings`) + a 300s revalidate ceiling, reading through new `lib/db/public.ts` — a cookie-free anon client (`unstable_cache` forbids `cookies()`; safe because these tables are public-read RLS). `React.cache` adds per-request dedup on `getProductBySlug`/`getStoreSettings`/`getCurrentUser`. **Write invalidation:** every mutating server action expires exactly the tags it touches via Next 16's `updateTag` (immediate expiry + read-your-own-writes; single-arg `revalidateTag` is deprecated in 16.2 and now a type error) — product/category/settings/review admin actions, admin order cancel, customer cancel, and checkout (stock changes). **Proxy:** the admin gate swapped `getUser()` (an Auth-server round trip per navigation) for `getClaims()` (local ES256 JWT verify against a ~10-min module-cached JWKS); `isAdmin` widened to accept User or claims. **Nav:** new group-level `app/(storefront)/loading.tsx` shimmer (Phase 4.3 skeleton palette) so navigations paint instantly; routes with closer-shaped skeletons keep theirs. **Product page:** profile + purchase-check reads parallelized with `Promise.all`. **Audited, already good (no change):** `next/image` with explicit dims/`sizes` everywhere, fonts self-hosted via `next/font` (`display: swap`), bundle within budget. **Measured (prod build, :3100):** warm TTFB ~8–10ms vs ~90ms cold (DB round trip gone); baseline CWV was already in budget (LCP 0.7–1.1s local, CLS 0.00) — now with real headroom; admin gate re-verified (unauth `/admin/orders` → 307 to sign-in). **Testing:** `bun test` 86/86 — and this pass unearthed a dead test file: `checkout/actions.test.ts` had silently failed to load since 4.10 (`server-only` unresolvable under bun). Fixed via `bunfig.toml` test preload (`test-setup.ts` stubs the `server-only` marker + `next/cache` invalidation no-ops) plus a rate-limit mock in the checkout tests (the in-process limiter tripped once all 10 tests actually ran). tsc clean; build green (all routes `ƒ`). *Follow-ups: (1) `unstable_cache` is per-instance — across serverless instances tags still invalidate correctly per-instance but each warms separately; a shared cache handler is a deploy-time concern; (2) DB-trigger-driven drift (anything an action forgets to tag) rides the 300s ceiling at worst; (3) E2E/visual suites not re-run this pass (unit + build + live smoke only) — run before deploy.* Absorbs the cross-cutting Performance item *(2.7)*.
- ✅ **4.18b — Skeleton/page alignment fix (user-flagged).** Loading skeletons didn't match their rendered pages. Full rundown of all fallbacks: **`ListingSkeleton`** — title block 36px→42px + real `gap-1.5`, sidebar 220px→230px, added the mobile "Filters" disclosure bar placeholder (the real 4.8 sidebar shows it below `lg`, so mobile navs jumped), card meta now mirrors `ProductCard` (`gap-[7px]`, 20px title line). **`ProductDetailSkeleton`** — rewritten: was `grid lg:grid-cols-2` vs the real `flex md:flex-row` (single-column skeleton vs two-column page across the whole md–lg range), thumbs now the real 4-col aspect-square grid (was 3 fixed 80px squares), breadcrumb `mb-[26px]`, `pb-[70px]`. **Group fallback `(storefront)/loading.tsx`** — rewritten from a generic breadcrumb+two-column card (matched nothing) to the full-bleed maroon-gradient hero band + centered content section that home (`Hero`) and every help/legal page (`HelpHeader`) actually open with. **Four new route-specific fallbacks:** `cart/loading.tsx` + `checkout/loading.tsx` (`max-w-[1180px]`, 44px heading, items + 360px summary card matching `CartSummary`/`CheckoutSummary`), `account/loading.tsx` (`max-w-[860px] py-12`, 32px header row + stacked cards, covers /account + orders + order detail), `order/[orderNo]/loading.tsx` (centered 680px confirmation: check circle + heading + summary card). All geometry copied from the real pages' classes. **Verified:** build green (all 7 loading chunks in `.next/server`), `bun test` 91/91, real-page geometry cross-checked in the browser (@1440, /shop + /cart); the transient fallback itself isn't capturable on localhost (~10ms renders) — worth a throttled re-check on the deployed host.
- ⬜ **4.19 — Enforce the COD toggle at checkout** once a second tender exists *(3.4)*; revisit sign-in-only checkout with real traffic data *(3.5)*. **Superseded by 5.3**, which enforces the toggle now (COD-only makes it the store kill-switch).

## Phase 5 — Admin console production readiness (from `docs/ADMIN_CONSOLE_AUDIT.md`, audit 2026-07-06)
Goal: clear the operational blockers found in the admin console audit (code review of `app/(admin)`/`components/admin`/`lib/admin`/`lib/db/admin-*` + live browser walkthrough of all 10 views, prod build, @1440/@390). Verdict there: **safe but not yet operable** — security model is production-grade; the gaps are operational. Item codes in *(parens)* reference the audit doc. 5A items should land before taking real customer orders.

### 5A — Pre-launch blockers
- ✅ **5.1 — Surface admin read failures.** New shared `lib/db/admin-read.ts` — `AdminRead<T> = { data: T; error: boolean }` + a `loadAdmin(label, load, fallback)` wrapper that runs the read, and on throw **logs `console.error("[admin-read] {label} failed:", err)` server-side** (closing the "zero logging" gap) and degrades to `{ data: fallback, error: true }`. All **nine view loaders** (`admin-{dashboard,orders,products,categories,coupons,messages,reviews,subscribers,analytics}.ts`) refactored from silent `try { … } catch { return EMPTY }` to `loadAdmin(…)`, returning the envelope; their **nine pages** now destructure `{ data, error }` and render the new client `components/admin/ui/AdminErrorBanner.tsx` (role="alert", admin-token maroon/red on cream, **Retry → `router.refresh()`**) above the view when `error` — an operator sees "Couldn't load" instead of a healthy-looking "0 orders today". The two **secondary** product reads (`getAdminProductById`, `getAdminCategories`, which feed the edit modal / category dropdown, not the banner) get the same `console.error` logging but keep their `null` / `[]` shape. `admin-metrics.ts` (sidebar badge counts — chrome, no banner surface) is **logging-only**: badges still degrade to zero, but the failure is no longer silent. New `lib/db/admin-read.test.ts` (**2/2**: success → `error:false`; throw → fallback + `error:true` + logged-once). **`bun test` 93/93; tsc clean; build green (all admin routes `ƒ`).** *Follow-ups: (1) no live browser pass forcing a real read failure yet — matches the admin bar (login-gated); the envelope + logging are unit-tested and the banner is wired uniformly. (2) `console.error` is the interim sink — swap for the structured logger when 4.11 (Sentry) lands, at the single `loadAdmin` call site + the three inline catches. Pairs with 4.11 observability *(C1)*.
- ✅ **5.2 — Order status emails.** Two new pure templates (unit-tested, same email-client-safe inline-table style as 4.6's `order-confirmation.ts`, reusing its `escapeHtml`): **`lib/email/order-status.ts`** `buildOrderStatusEmail({kind, orderNo, customerName, totalPaise, orderUrl})` — one builder, per-kind copy for the three statuses that matter to a COD customer (**Shipped** "on its way" + keep-amount-ready cue, **Delivered** "thank you", **Cancelled** "you haven't been charged"), each with its own accent + heading; **`lib/email/admin-alert.ts`** `buildNewOrderAdminEmail(...)` — internal new-order alert (order no, customer, city/state, item count, COD total, "Open order queue" link). **`lib/email/send.ts`** gains a shared `queue(to, message)` primitive (extracted from 4.6's `after()`/detached-fallback pattern) + `queueOrderStatusEmail` (links to the public `ROUTES.order` page) and `queueNewOrderAdminEmail` (recipient = `ADMIN_ALERT_EMAIL` env, else `STORE_INFO.email`; links to `ROUTES.adminOrders`) — both no-ops without `RESEND_API_KEY`, both best-effort (a mail hiccup never fails an applied change). **Wired:** admin `setOrderStatus` (`orders/actions.ts`) queues the customer email when the new status ∈ {Shipped, Delivered, Cancelled} — reads the order authoritatively (order_no/email/name/total via admin RLS) after the RPC succeeds; customer `cancelMyOrder` (`account/orders/actions.ts`) queues the **Cancelled** email (own-row RLS read); checkout `submitCheckout` queues the **admin new-order alert** alongside 4.6's customer confirmation. **Tests:** `order-status.test.ts` (each kind carries order no/total/link in html+text, COD/cancel copy, XSS-escape, blank-name fallback) + `admin-alert.test.ts` (fields, item-count pluralise, escape) + checkout test asserts the admin alert queues once on success / never on failure. **Incidental 5.1 hardening (build surfaced it):** the new `loadAdmin` catch was swallowing Next's `DynamicServerError` (control-flow during static-gen probing) and logging it as a read failure — added `unstable_rethrow(err)` at the top of the catch so framework signals (DynamicServerError/redirect/notFound) propagate and only real read failures degrade; build log is clean again. `.env.example` documents `ADMIN_ALERT_EMAIL`. **`bun test` 102/102; tsc clean; build green (all routes `ƒ`, no dynamic-usage log noise).** *Follow-ups: (1) needs a real `RESEND_API_KEY` + verified domain (4.13) and `ADMIN_ALERT_EMAIL` set — until then every send is a no-op, unchanged behaviour; no live send exercised yet. (2) Confirmed/Packed intentionally don't email (audit scope = Shipped/Delivered/Cancelled). (3) status emails link to the public confirmation page (`ROUTES.order`), not the login-gated account detail, so they open without sign-in. Pairs with 4.11 observability *(C2)*.
- ✅ **5.3 — Make the COD toggle real.** The Settings "Cash on Delivery" toggle (`setting.cod_enabled`) now actually gates ordering. **Server (the authority):** `0025_cod_toggle.sql` re-creates `place_order` (0019 body verbatim) to read `cod_enabled` alongside the shipping config it already loads and **raise `COD_DISABLED`** before writing anything when off — since COD is v1's only tender, this is the store's order kill-switch, and a direct RPC call can't bypass it. **Checkout action:** `submitCheckout` maps `COD_DISABLED` to a specific "we've paused online orders — please contact us" message instead of the generic decline. **UI (mirrors, doesn't gate):** `codEnabled` threads `getStoreSettings()` → checkout `page` → `CheckoutView` → `CheckoutForm`; when off, the COD row renders muted with a "Paused" tag (matching the Razorpay "coming soon" treatment), a maroon/red notice appears under the payment options, `onValid` blocks before the round-trip, and `CheckoutSummary`'s button reads "Orders paused" + disabled with paused helper copy. **Tests:** +1 (`actions.test.ts` — `COD_DISABLED` → paused message, no emails queued). **`bun test` 111/111; tsc clean; build green (all routes `ƒ`).** *Follow-ups: (1) enforcement verified via unit test + the RPC guard, not a live toggle-off browser pass (checkout needs a signed-in session + cart). (2) When Razorpay lands (deferred), the "only tender" framing changes — COD off should then fall back to card, not pause the store. Absorbs 4.19 *(C3)*.*
- ✅ **5.4 — Confirmations on destructive actions.** New shared **`components/admin/ui/ConfirmDialog.tsx`** — a generic admin `alertdialog` (overlay + card, focuses the dismiss button on open, closes on Escape, both blocked while pending; destructive maroon/red confirm) generalised from the Team page's single-purpose `ConfirmRemoveDialog` (title/body/confirm/pending/dismiss labels + `isPending`/`error`/`onConfirm`/`onClose` props). **Wired to the two actions that fired on a single click:** **order Cancel** (`OrdersView` now holds a `confirmingCancel` flag — the drawer's Cancel button arms the dialog instead of running `runChange("Cancelled")`; the dialog names the order no and warns the cancel is terminal + returns stock) and **subscriber Remove** (`SubscribersView` holds the pending row — the `×` arms the dialog naming the email instead of removing on click). **DRY:** `ConfirmRemoveDialog` deleted and the Team revoke migrated onto the shared `ConfirmDialog` (its sole caller), so the console has one confirm dialog. **Category Delete already had a deliberate two-step** (`CategoryModal`'s `confirmDelete` "Delete"→"Confirm delete?" inline swap, inside its own modal where a nested dialog would be awkward) — left as-is; it already satisfies the no-single-click bar. The server RPCs (`admin_set_order_status` Cancel, `admin_remove_subscriber`, `admin_revoke`) remain the real authority — the dialog only guards the misclick. **`bun test` 111/111; tsc clean; build green (all admin routes `ƒ`).** *Follow-up: live browser pass of the two new confirm flows not run this pass (login-gated); the dialog is a direct generalisation of the already-browser-verified Team `ConfirmRemoveDialog` shell, tsc+tests+build green *(H1)*.
- ✅ **5.5 — Order search.** Queue-local search across **order no / customer name / phone / email**, URL-driven (`?q=`) like the existing `status`/`page` params so it's shareable + refresh-safe. **Data layer** (`listAdminOrders`): new `search` opt → `buildOrderSearchOr` builds a PostgREST `.or(order_no.ilike…,customer_name.ilike…,customer_phone.ilike…,customer_email.ilike…)` (mirrors the proven `admin-products` search), **sanitising `%,()`** first — they'd otherwise act as ilike wildcards or break the `or()` grammar (returns null when nothing survives). The filter is applied to **every** query — the page of rows **and** each per-status head-count — so the tab counts and pagination reflect the filtered set, not the whole queue; `AdminOrdersPage` gains `search`. **UI** (`OrdersView`): a `role="search"` form above the tabs (search icon + input seeded from the served value, Search + conditional Clear buttons) that `router.push`es `?q=` preserving the active status tab and resetting to page 1; a result-count line ("N orders matching …" / "No orders match …"); `hrefFor` + the pager now thread `q` so paging/tab-switching keep the search. **Topbar** (`AdminTopbar`): the previously-dead "Search orders, products…" box is now a real form submitting to `/admin/orders?q=` — relabeled **"Search orders…"** (honest: only orders are wired; product search stays out of scope). **Verified against live DB** (read-only probes mirroring the `.or()` shape): partial order-no `1010`→1, name `chetan k`→1, phone `997277`→1, plus-addressed email `+e2e`→6, `zzz`→0. **`bun test` 111/111; tsc clean; build green (all routes `ƒ`).** *Follow-ups: (1) date filter + CSV export (named in the same audit gap) deferred — search is the "customer calls about JR-2607" headline; export overlaps the subscribers CSV pattern; (2) no dedicated unit test for `buildOrderSearchOr` (server-only module; parity with the untested `admin-products` search) — the sanitiser + live probes cover it; (3) live admin-UI browser pass not run (login-gated), logic DB-verified *(H4, M2)*.
- ✅ **5.6 — Unify brand truth + clear sample GSTIN.** Canonical name = **RJ Jewellers** (user decision — matched the existing `STORE_INFO`/sidebar/email wordmark rather than the prototype filename's "JR"). **`app/layout.tsx`** now derives the tab title + template from `STORE_INFO.name` (`${name} — Bridal & Fine Jewellery` / `%s · ${name}`) so titles can't drift from the sidebar/emails again — the descriptive suffix stays inline as editorial SEO copy. **Codebase sweep:** every remaining customer-visible "JR Jewellers"/"JR JEWELLERS" → "RJ" — storefront metadata descriptions (contact/care/faq/shop/about/shipping/[category]/product), the About + help-content prose, the **Hero wordmark**, the sign-in prompt, the OG-image alt text, and `settings.ts`'s `storeName` fallback default; the only two left are the `Header`/`Footer` comments that name the actual prototype file `refereces/JR Jewellers Storefront.html` (a real filename, not brand copy). **Live DB settings row corrected** (single-row data fix — settings are editable content, not schema, so no migration): `store_name` JR→**RJ Jewellers**, `support_email` `owner@jrjewellers.in`→**`care@rjjewellers.in`** (matches `STORE_INFO.email`), sample **`gstin` `27ABCDE1234F1Z5` → null** (GSTIN not issued yet, mirroring `STORE_INFO.gstin`). **`bun test` 111/111; tsc clean; build green.** *Follow-ups: (1) `getStoreSettings` is `unstable_cache`'d (`settings` tag, 300s ceiling from 4.18) — the direct DB write wasn't through a tagged action, so a warm instance shows the old name until the 300s revalidate (or a Settings-page save, which does `updateTag`); acceptable for a one-time correction. (2) `setting.phone` is still the masked placeholder `+91 99XXX XXX21` — real value is owner data-entry at launch, out of 5.6 scope *(M1, M6-prep)*.

**Phase 5A complete** — all pre-launch blockers (5.1–5.6) cleared; the console is now safe *and* operable for taking real COD orders.

### 5B — High-priority hardening
- ✅ **5.7 — Dialog a11y.** New shared hook **`hooks/useDialog.ts`** (`useDialog<T>({ isOpen, onDismiss, isPending })` → a ref for the dialog element) centralises the missing a11y wiring: moves focus **into** the dialog on open (first focusable, else the container), **traps** Tab/Shift+Tab inside it, **closes on Escape** (suppressed while an action is pending), **locks body scroll** (restoring the prior inline value so stacked dialogs unwind), and **returns focus** to the trigger on close. Applied to all five admin dialogs — `ProductModal` and `OrderDrawer` (which previously had **no** `role="dialog"`/`aria-modal` at all) now get them + `tabIndex={-1}`; `CouponModal`, `CategoryModal` and the shared `ConfirmDialog` adopt the same hook (ConfirmDialog's hand-rolled `useEffect`/`dismissRef` focus+Escape logic is deleted in favour of it — DRY). The closed `OrderDrawer`'s backdrop close button is now `aria-hidden` + `tabIndex={-1}` so it leaves the a11y tree/tab order when the drawer is shut. **tsc clean; `bun test` 111/111; build green (all routes).** *Verified by type-check + build; live keyboard/browser pass needs admin login (like 3.4) — deferred to the next authed admin session. Onboarding note: `useDialog` is storefront-reusable if a modal lands there later.*
- ✅ **5.8 — Audit trail.** New migration `supabase/migrations/0026_admin_audit_log.sql` (applied to `naolegptozpaiojozzcy`): append-only **`admin_audit_log`** table (`actor_id`→auth.users + denormalised `actor_email`, `action`, `entity_type`, `entity_id`, `summary`, `meta jsonb`, `created_at`), RLS-enabled with an **is_admin() read policy and no insert policy** (only the trigger writes). **Design choice — one `AFTER` trigger function (`tg_admin_audit`, SECURITY DEFINER), not inserts re-emitted inside each write RPC** (the tracker's original wording). Attaching triggers to the four tables (`order`/`product`/`setting`/`review`, entity passed via `tg_argv`) means the four production `SECURITY DEFINER` RPCs stay byte-for-byte untouched (zero risk to checkout/admin writes), yet the real actor is still captured because `auth.uid()` resolves inside those definer functions (JWT GUC is session-level). An **`is_admin()` gate logs only admin-context changes** — customer checkout stock-decrements and self-service cancels are skipped — and any future admin write path is covered for free. Per-entity semantics: order/review → `status` transitions (`Confirmed → Shipped`); setting → `setting.update` with a changed-columns before/after diff; product → `product.create` / `product.price` / `product.update` / `product.stock` (stock-only movements, incl. restock-on-cancel, labelled distinctly so they don't read as edits). Complements the existing `admin_role_audit` (0023, team grants/revokes — left as-is). **Verified** against the live DB in rolled-back transactions (spoofed JWT claims): admin `setting.update` logs actor id+email + a `store_name` before/after diff; a non-admin write logs **nothing** (gate holds); an admin price edit logs `product.price` with slug + paise diff. *Follow-ups: (1) no admin UI reads the log yet — a read-only "Activity" view (reuse the Team page's audit list styling) is a later task; hence `lib/db/types.ts` regen is deferred until a consumer exists. (2) order/review branches share the proven insert path but weren't separately exercised live (needs a non-terminal order + an authed admin session, like 3.4).* *(H3)*
- ✅ **5.9 — Coupon edit/delete.** New migration `supabase/migrations/0027_coupon_delete.sql` (applied to `naolegptozpaiojozzcy`): **`admin_delete_coupon(p_id uuid)`** SECURITY DEFINER RPC, is_admin()-gated with a `COUPON_NOT_FOUND` guard, mirroring `admin_delete_category` (hard delete is safe — `coupon` has no child rows and orders store the code as plain text, not a FK). New `deleteCoupon` server action (`coupons/actions.ts` → the RPC + `revalidatePath`; reuses the existing `messageFor` COUPON_NOT_FOUND mapping). **CouponModal now does both create and edit**: takes an optional `coupon` prop, prefills every field by reversing the action's rupees→paise / value encoding (`valueStringFor`, `paiseToRupeeString`, expiry `slice(0,10)`), passes `id`+`isActive` through so editing preserves the toggled state, retitles to "Edit Coupon" / "Save Changes", and shows a two-step **Delete** button (mirrors CategoryModal's confirm-on-second-click). **CouponsView** gains a per-row **Edit** button (new column; `modal` state is `"new" | AdminCouponRow | null`) that opens the modal in edit mode. `lib/db/types.ts` got a **single additive** `admin_delete_coupon` signature — deliberately *not* a full regen, because the newer generator (Postgrest 14.5) flips several upsert RPC `p_id` args from nullable to non-null and would break four unrelated create-path call sites; the audit-log table (5.8) needs no types until a UI reads it. **Verified:** `admin_delete_coupon` live in a rolled-back txn (1 coupon → 0; bogus id → `no_data_found`); `bun test` 111/111; tsc clean; build green (all routes). *Live browser edit/delete pass needs an admin login (like 3.4). Coupon changes aren't in the 5.8 audit scope (that covers order/product/setting/review) — could be added later.* *(M4)*

### 5C — Post-launch hygiene (from the audit's MEDIUM/LOW tiers)
- ✅ **5.10 — Bound the list queries.** All five previously-unbounded admin surfaces (Reviews, Messages, Subscribers, Coupons, Categories) now read **one server-rendered page**, reusing the orders/products pattern — killing the full-table scans on the monotonically-growing tables. **Shared pager:** new `components/admin/ui/AdminPager.tsx` (one "Showing X–Y of N" range line + Prev/numbered/Next, `hrefForPage` per surface, renders nothing when empty) — a single implementation extracted from the duplicated inline pagers (orders/products keep their own for now; migrating them is a low-value follow-up). **Page sizes** live in each client-safe lib (`ADMIN_{REVIEWS:12, MESSAGES:12, SUBSCRIBERS:20, COUPONS:10, CATEGORIES:12}_PAGE_SIZE`). **Reviews/Messages** — the filter is now **URL-driven** (`?status=`/`?page=`, `<Link>` tabs like orders, defaulting to the Pending/New queue via new `toReviewFilter`/`toMessageFilter` coercers); tab counts come from **exact head-counts per status** (scale-safe), the page is `.range()`-d, and reviews resolve each page row's product name from a **bounded** `product.in(pageIds)` read (not a full scan). **Coupons** — `.range()` + exact count (oldest-first, BRIDE20 stays top). **Categories** — `.range()` + exact count, and the per-collection product tally is now a **bounded** `product.in(pageCategoryIds)` read instead of scanning the whole catalogue. **Subscribers** — email **search is URL-driven** (`?q=`, ilike-sanitised server-side) + `?page=`; the three KPI cards are computed from **aggregate reads** (exact total, this-week count, per-source head-counts) independent of the page, so they stay true for the whole list; **Copy emails / Export CSV** now pull the entire list on demand via a new `exportSubscribers()` server action (`requireAdmin` → `getAllSubscribers`, capped at 5000) so they still cover the full mailing list, not just the visible page. Each data fn returns a `{rows, …, page, pageCount, total}` envelope through the existing `loadAdmin` error wrapper (5.1), and every page reads `searchParams` and hands the envelope to its client view. **Live-verified the aggregate shapes** against `naolegptozpaiojozzcy` (reviews All 11 / pending 3 / approved 8; messages All 5 = 2+2+1; subscribers total 2 / this-week 2 — all matching the modules' head-count logic; current data sits under every page size so the queries are exercised but don't yet visibly page). **`bun test` 111/111; tsc clean; build green (all admin routes `ƒ`).** *Follow-ups: (1) no live admin browser pass forcing a multi-page render (login-gated) — matches the 3.3–5.9 admin bar; the pager is a byte-faithful copy of the 4.17-browser-verified products pager and the range/`.range()`/`ceil` math mirrors the verified admin-orders/products path. (2) an out-of-range `?page=` (e.g. after a filter shrinks results) returns empty rows with a correct pager but no active page — same minor edge the existing admin-orders/products lists carry (storefront `getProductsPage` clamps; admin lists don't). (3) orders/products could adopt `AdminPager` to retire their inline pagers — pure DRY cleanup, deferred. (4) subscribers search/export ignore each other by design (search filters the browse table; export is the whole list) *(M3)*.
- ✅ **5.11 — Dashboard KPI consistency.** The mismatch is resolved + labelled: **"Orders Today" stays the activity headline (every order placed today, all statuses); "Revenue Today" excludes cancelled — and the gap is now explained** rather than silently inconsistent. `getDashboardData` (`lib/db/admin-dashboard.ts`) splits the IST-day bucketing into a single partition — `ordersByDay` counts every order, and a cancelled order now increments a new `cancelledByDay` tally **instead of** `revenueByDay` (was: counted in orders, silently dropped from revenue with no signal) — and exposes `kpis.cancelledToday`. `DashboardView` renders a muted red **"{n} cancelled"** line under the Orders-Today delta **only when `cancelledToday > 0`**, so on a clean day the card is unchanged. **Verified live** against `naolegptozpaiojozzcy` (today: 1 order / 0 cancelled / revenue ₹778 — headline 1, no cancelled note, revenue matches; the note branch appears only once a same-day order is cancelled). **tsc clean; build green (all admin routes `ƒ`); `bun test` 111/111.** *Follow-up: order/revenue deltas still compare like-for-like within each metric (orders all-vs-all, revenue non-cancelled-vs-non-cancelled) — consistent per metric; no live browser pass of the cancelled-note branch (login-gated + needs a same-day cancellation) — matches the admin bar *(M5)*.
- ✅ **5.12 — GST invoice / packing slip.** New print surface at **`/admin/orders/[orderNo]/print`** — **separate invoice and packing-slip documents on one route**, selected by `?doc=` (invoice is the default; `?doc=packing-slip` for the slip), so each prints independently *(reworked on user request from the original one-two-page-document shape; `lib/admin/print.ts` holds `PRINT_DOCS`/`toPrintDoc`, the on-screen toolbar cross-links the two docs)*. **Route architecture:** the page lives in a new **`(print)` route group** (sibling of `(console)`) so the sidebar/topbar chrome never ends up on paper; because it skips the console layout it **re-runs `requireAdmin` itself** (+ the proxy's coarse /admin gate) — **verified live against the prod build: unauthenticated request → 307 → `/admin/sign-in?next=…/print`**; unknown order numbers 404; `robots` noindex. **Data:** `lib/db/admin-orders.ts` gains `getAdminOrderByNo(orderNo)` (same admin-RLS SELECT, null → 404) — and the queue's inline row mapper is extracted to a shared `mapOrderRow` (pure refactor, listAdminOrders behaviour unchanged). **`OrderPrintView`** (server component; the only client island is a tiny `PrintButton` → `window.print()`): paper-styled (white ground, ink hairlines, Tailwind `print:` variants; on-screen toolbar with Back/Print is `print:hidden`). **Invoice:** store block from `STORE_INFO` + **GSTIN from Settings when set — the title is "TAX INVOICE" only with a GSTIN, plain "INVOICE" otherwise** (honesty rule: can't issue a tax invoice unregistered); invoice no = order no, date, payment; Bill-to/Ship-to; item table (qty / unit price derived `lineTotal/qty` — exact, DB stores unit×qty / amount); Subtotal/Discount/Shipping/Grand total + "inclusive of all applicable taxes" note (prices are stored tax-inclusive — **no fabricated CGST/SGST split**); status-aware payment line (COD payable / paid / cancelled) + a CANCELLED badge on cancelled orders. **Packing slip:** big bordered Deliver-to block (the parcel face), phone, **"COD — COLLECT ₹X" box** while payment is outstanding, price-free item list + parcel item count, fragile note. **Entry points:** `OrderDrawer` gains separate **"Print invoice"** and **"Print packing slip"** buttons (new tab) above the AWB stub; `ROUTES.adminOrderPrint(orderNo, doc)` added. **tsc clean; `bun test` 111/111; build green (`/admin/orders/[orderNo]/print` `ƒ`).** *Follow-ups: (1) CGST/SGST break-up needs the store's GST rate — add once GSTIN + rate are known (single place: the invoice totals block). (2) No authed browser/print-preview pass yet (login-gated) — layout uses the same Tailwind utilities as the verified console; check page breaks in print preview on the first real use. (3) 5.15's courier address-label can reuse the packing-slip Deliver-to block *(M6)*.
- ✅ **5.13 — Small fixes batch.** **(M7) Analytics deep-link:** the products-row chart icon now links `/admin/analytics?product=<id>` — the analytics page reads `searchParams`, passes `initialProductId` to `AnalyticsView`, which seeds its detail-view selection (unknown ids fall back to the list since `selected` derives via `find`; mirrors the existing products `?edit=` pattern, and Back is client state just like it). **(L1) Pager windowing:** `AdminPager` now windows page numbers — always 1, last, current ±1, runs collapse to "…" (a gap of exactly one page renders the number instead; ≤7 pages shows all) — and **orders + products migrated onto `AdminPager`**, retiring their duplicated inline pagers (the 5.10 DRY follow-up; all 7 list surfaces now share one windowed pager). **(L2)** topbar search input got `id="admin-order-search"` (`name="q"` was already there). **(L3)** the Import-CSV stub button ("coming soon" notice, no real feature behind it) is **removed** — honest UI; trivially restorable from git when a real importer ships — along with its now-orphaned notice state/banner. **(L4)** already satisfied: the sidebar footer email has carried `title={adminEmail}` since the truncation fix — verified, no change. **tsc clean; `bun test` 111/111; build green (all admin routes `ƒ`).** *Follow-ups: (1) `pageItems` windowing is verified by inspection, not unit test — it's private to the component; extract to a lib if a regression ever bites. (2) no live multi-page browser pass (login-gated + data sits under every page size) — matches the admin bar.* (Rate-limiter store *(L5)* stays with 4.13 deploy.)

### 5D — Day-to-day operations additions (audit §5; top-5 first)
- ✅ **5.14 — New-order alerts.** **Email half was already live** (shipped with 5.2, verified this pass): checkout's `placeOrder` queues `queueNewOrderAdminEmail` (`lib/email/admin-alert.ts`, tested) to `ADMIN_ALERT_EMAIL` (falls back to the store inbox) via `after()` — push, not polling; no-op without `RESEND_API_KEY`. **Bell wired this pass:** the topbar bell now takes the same `AdminNavCounts` the console layout already fetches for the sidebar (pending orders / reviews to moderate / new messages — no new queries) and renders a **count badge** (total, "99+" cap) plus a **"Needs attention" dropdown**: one row per non-empty queue with its count, deep-linking Orders→`?status=Pending`, Reviews/Messages→their default queue tabs; empty state "All caught up — nothing pending"; backdrop click closes; `aria-haspopup`/`aria-expanded`/count in the aria-label. Counts are per-server-render (fresh on every navigation), **not a live socket — honest scope**: same freshness as the sidebar badges. **tsc clean; `bun test` 111/111; build green.** *Follow-ups: (1) live push (Supabase Realtime on `order` insert) if refresh-on-navigation proves too slow in practice; (2) no authed browser pass (login-gated) — matches the admin bar.*
- ✅ **5.15 — Order-drawer workflow tools.** **Call/WhatsApp:** the drawer's Customer card gains Call (`tel:`) + WhatsApp buttons (Messages' visual pattern). New pure builders in `lib/whatsapp.ts` (**5 new unit tests**, 116 total): `customerWhatsappUrl(phone, msg)` — first wa.me link pointed *at the customer* (10-digit → `91` prefix; null on unusable numbers → button hides) — and `codConfirmationMessage` ("Namaste {name}… order {no} for {₹}… reply YES to confirm"), prefilled **only while the order is Pending COD** (the confirm-before-dispatch moment); other statuses get a neutral opener naming the order. **Customer context line:** `AdminOrderRow` gains `customerOrderCount`/`customerCancelledCount` — tallied per customer **by phone** via one bounded `select(customer_phone,status).in(page's distinct phones)` companion read in `listAdminOrders` (5.10 pattern; `getAdminOrderByNo` does the single-phone equivalent) — rendered under the name: "First order from this customer" / "{n} orders from this customer **· {c} cancelled**" (cancelled in red — the COD-risk signal; we track cancellations, not courier refusals, so the label says cancelled). **Live-verified** against `naolegptozpaiojozzcy`: phone …5678 → 7 orders/3 cancelled, …7455 → 1/0 — matches the tally logic. **Address label:** already served by 5.12's packing slip (the bordered Deliver-to block is the parcel face; button in this same drawer) — no new surface. **tsc clean; `bun test` 116/116; build green.** *Follow-ups: (1) MessagesView still carries its own inline wa.me builder — could adopt `customerWhatsappUrl` (DRY, deferred); (2) courier-refused tracking needs a `Refused` status or Shiprocket webhook — revisit with 5.15's courier phase; (3) no authed browser pass (login-gated) — matches the admin bar.*
- ✅ **5.16 — Order notes + status timeline.** **Folded into 5.8's audit log** (no new table): notes are `order.note` rows in `admin_audit_log`, written by new **`admin_add_order_note` RPC (migration 0028, applied live)** — SECURITY DEFINER + `is_admin()` gate (the table stays INSERT-sealed otherwise), validates non-empty/≤500, `ORDER_NOT_FOUND` on bad order-no, and **returns the inserted row as jsonb** so the UI appends without a refetch; `NOT_ADMIN` gate **live-verified** (probe raised 42501). Pure `normalizeOrderNote` in `lib/admin/order-notes.ts` mirrors the rule client-side (**4 new tests, TDD-first → 120 total**). **Timeline read:** `AdminOrderRow` gains `events: OrderEvent[]` — synthetic "Order placed" (from `created_at`) + `order.status`/`order.note` audit rows, fetched as a second bounded companion read in `listAdminOrders` (page's order-nos, parallel with 5.15's phone read; single-order `.eq` in `getAdminOrderByNo`), IST date+time labels. **Drawer:** new "Timeline & notes" card (dot-and-rail list; notes render as cream boxes, `atLabel · actorEmail` meta) + note composer (textarea, maxLength 500, `addOrderNote` server action → returned event appended to the drawer snapshot via `onNoteAdded`; status advances append a "Just now" local entry mirroring the trigger's row). `admin_audit_log` + RPC added to `lib/db/types.ts`. **tsc clean; `bun test` 120/120; build green.** *Follow-ups: (1) customer self-cancels aren't audited (0026's `is_admin()` gate skips them) so they show no timeline entry — fold in if a `customer.cancel` audit action ever lands; (2) pre-0026 status changes have no audit rows — old orders show only "Order placed"; (3) happy-path RPC + composer need the authed browser pass (login-gated) — matches the admin bar.*
- ✅ **5.17 — Daily digest email.** `getDashboardData` couldn't serve this (cookie-RLS-bound, computes *today*), so the digest gets its own path following the app's unauthenticated-but-gated RPC pattern (place_order / get_order_confirmation): **`get_daily_digest(p_secret)` (migration 0029, applied live)** — SECURITY DEFINER, gated on a secret in new RLS-sealed `app_secret` table (no policies; only definer fns read it), returns **aggregates only, no PII**: yesterday-IST's orders/cancelled/revenue (cancelled excluded, mirrors dashboard), current pending count, low-stock ≤5 (count + worst 5). **Live-verified**: NOT_CONFIGURED (no secret row), BAD_SECRET 42501 (wrong value), happy path with a temp probe secret returned real aggregates (2026-07-06: 4 orders/2 cancelled/₹6,076.80/1 pending) — probe row deleted. **Builder**: `lib/email/daily-digest.ts` `buildDailyDigestEmail` (**6 tests, TDD-first → 126 total**; escapes product names, cancelled note conditional, plural rules). **Sender**: `sendDailyDigestEmailNow` in send.ts — *awaited* (unlike the queued sends) so the cron sees failures; `sendEmail` now returns success. **Route**: `GET /api/cron/daily-digest` — bearer must equal `CRON_SECRET` env, RPC re-checks the same value against `app_secret` (two gates, one secret); zod-validates the payload; 503 unset / 401 bad bearer / 502 RPC-or-send failure / 200 `{ok,date,orders}`. **Route probed against a local prod build**: 401/401/502(NOT_CONFIGURED) exactly as designed. **Deploy wiring documented** (docs/DEPLOY_DOKPLOY.md §4.3 + .env.example): `openssl rand -hex 32` → CRON_SECRET env + one `app_secret` insert + Dokploy cron `30 16 * * *` (22:00 IST) curl. **tsc clean; 126/126; build green (route registers `ƒ`).** *Follow-ups: (1) actual send untested until RESEND_API_KEY + secret exist at deploy — first scheduled run is the test (route surfaces failures as 502); (2) secret comparison isn't constant-time — acceptable for a long random token; revisit with Vault if secrets multiply.*
- ◐ **5.18 — Ops niceties (as demand appears).** **First slice shipped** (the three highest-value, low-risk items): **(1) Pending ageing chips** — pure `pendingAge` in `lib/admin/order-aging.ts` (**4 tests**: <12h null, 12–24h amber "12h+", ≥24h red "24h+", NaN-safe) rendered under the status pill on Pending rows only (`suppressHydrationWarning` — chip depends on `Date.now()`, SSR/hydrate can straddle a threshold). **(2) "View on storefront" links** — `AdminProductRow` gains `slug` (additive select); products rows get an external-link icon → `/product/[slug]` (`target=_blank`, `noopener noreferrer`), hidden for Draft (the only storefront-hidden status per `STOREFRONT_VISIBLE_STATUSES`); actions header now "Actions" (w-80px). **(3) Orders CSV export for the CA** — `getAllOrdersForExport` (capped 5000, newest first, flat: order_no / IST date `YYYY-MM-DD` / status / payment / customer / phone / email / city / state / pincode / coupon / money in **INR decimals**) behind new `exportOrders` action (requireAdmin); "Export CSV" button beside search downloads `orders.csv` client-side (subscribers pattern). `csvCell` extracted to shared `lib/utils/csv.ts` (+`csvRow`, **5 tests**; SubscribersView now imports it — DRY at second consumer). **tsc clean; `bun test` 135/135; build green.** **Remaining (demand-gated, unshipped):** bulk-confirm; inline stock adjust; duplicate product; per-product low-stock threshold; COD reconciliation view; coupon performance numbers; featured-product ordering; review replies; undo toasts; admin 2FA (Supabase MFA). *Follow-up: ageing thresholds (12/24h) are constants — make them settings if the operator's rhythm differs.*

## Phase 6 — Operator feedback batch (17 items, validated 2026-07-08)

User-identified changes, validated against the codebase and ordered quick-wins-first. Sizes: S < half a session · M ~ one task turn · L = multi-turn.

**Quick wins**
- ✅ **6.1 (S) — Bell dropdown doesn't dismiss on click-away** *(item 2)*. Root cause: the `fixed inset-0` backdrop sat inside the topbar's stacking context, so higher-z siblings swallowed the click. Fixed: backdrop removed; document-level `pointerdown` outside-click (wrapper `ref.contains`) + **Escape** now dismiss — same semantics as `useDialog`. Item links still close on navigate. tsc clean; 135/135. *Authed browser pass deferred as usual.*
- ✅ **6.2 (S) — Coupon search filter** *(13)*. URL-driven `?q=` on code (shareable, survives refresh): `listAdminCoupons` gains sanitised `ilike` on `code` applied to rows + exact count so the pager stays honest; `AdminCouponsPage.search`; toolbar search box (input seeded from URL + Search/Clear, orders pattern) beside Create Coupon; pagination links carry the active `q`; search-aware empty state. tsc clean; 135/135; build green.
- ✅ **6.2b (S) — Debounced search everywhere** *(user follow-up to 6.2)*. All four table searches (orders, products, coupons, subscribers) now filter as-you-type: new shared `components/admin/ui/AdminSearchBox.tsx` (350ms debounce, fires only when the trimmed term differs from the served URL value, `router.replace` so typing doesn't spam history; clearing the input resets the filter — Search/Clear buttons and per-view forms deleted, incl. subscribers' local SearchIcon). URLs stay the source of truth (`?q=`/`?search=` contracts unchanged, pager links keep the term). Topbar box left as-is (it's a jump-to-orders shortcut, not a table filter). tsc clean; 135/135; build green.
- ✅ **6.3 (S) — Custom plating options** *(9)*. Validation caught the real blocker: the **upsert action whitelisted** plating to the three defaults (`PLATING_OPTIONS.filter`) — custom values would have been silently dropped (the 0008 RPC itself stores arbitrary strings). Action now sanitises instead: trim → drop empties → dedupe → length ≤ `MAX_PLATING_OPTION_LEN` (40) → cap `MAX_PLATING_OPTIONS` (8), bounds shared from `lib/admin/product-status.ts`. Modal: default chips unchanged; custom finishes render as removable maroon pills (surface on edit too); "Add a custom finish" input + Add button (Enter adds without submitting, case-insensitive dupe guard, hidden at the cap). **Storefront disconnect found & fixed (user report):** the tone selector read seed-era `product_option` rows (no console edit path) — admin plating edits *never* reached the storefront. `getProductBySlug` now synthesizes the selector from `product.plating_options` (label = value = stored string; `product_option` embed dropped, table kept). **Migration 0030 (plating backfill: `product_option` labels → empty `plating_options`) is WRITTEN BUT NOT APPLIED — Supabase MCP disconnected; apply before the next deploy or pre-0009 seeded products lose their tone selector.** tsc clean; 135/135; build green.

**Orders cluster**
- ✅ **6.4 (M) — Manual AWB** *(5)*. **Migration 0031 (applied + live-verified)**: `admin_set_order_awb(p_order_id, p_awb)` SECURITY DEFINER RPC — `is_admin()` gate, trim + 1–40 chars + `^[A-Za-z0-9][A-Za-z0-9 /_-]*$`, `ORDER_TERMINAL` on Delivered/Cancelled, revoke public/grant authenticated; `admin_set_order_status` re-created (0019 body) with an `AWB_REQUIRED` raise on →Delivered while `awb` is null/blank. Client mirror `lib/admin/awb.ts` (`normalizeAwb`, `AWB_MAX_LEN = 40`; 6 tests). New `setOrderAwb` action (`requireAdmin` + normalize + RPC + friendly error map incl. `INVALID_AWB`); `messageFor` gains the `AWB_REQUIRED` message. Drawer: `AwbCard` (keyed by order id) replaces the disabled Shiprocket stub — input + Save/Update (disabled while unchanged), amber "Required before marking this order Delivered" hint on Shipped-without-AWB, read-only value once terminal; saves lift via `onAwbSaved` into the drawer snapshot. **Live probes (rolled back)**: NOT_ADMIN, INVALID_AWB (`bad#awb`), ORDER_TERMINAL (Delivered order), AWB_REQUIRED (Shipped→Delivered sans AWB), then happy path — AWB trims + saves and Delivered succeeds.
- ✅ **6.4b (S) — Customer-facing AWB (user request 2026-07-08: "show it in storefront … so the user can track the order themselves")**. `MyOrderDetail` gains `awb` (already readable under the 0004 "customer reads own orders" RLS — column added to the select, no policy change). `/account/orders/[orderNo]` shows a **Tracking card** (storefront chrome, hidden when no AWB or Cancelled): "AWB {n}" + new `CopyAwbButton` client island (clipboard write, "Copied ✓" flash, silent no-op fallback) + "use this number on your courier's tracking page" line — **no courier link fabricated** (courier is unknown; 4.1 truth-fix precedent). **Shipped email** now carries a tracking row: `OrderStatusEmailInput`/`QueueOrderStatusInput` gain optional `awb`, rendered in text + HTML (escaped) on Shipped only (2 tests → 171); `setOrderStatus`'s notify read selects `awb` and passes it through. Confirmation page untouched (no AWB exists at placement). tsc/171/build green.
- ✅ **6.4c (S/M) — Tracking link (user request 2026-07-08: "add a tracking link … in storefront just show the link as awb")**. **Migration 0032 (applied + live-probed)**: `order.tracking_url text` + `admin_set_order_awb` re-created with a third param `p_tracking_url default null` (2-arg overload dropped for unambiguous PostgREST resolution) — blank clears, non-blank must be http(s) ≤300 chars (`INVALID_TRACKING_URL`; `javascript:` probe rejected; set→clear round-trip verified pinned to one order_no after a `limit 1`-across-two-Shipped-rows false alarm — **operator was live-testing the drawer mid-session** (JR-260707-1012 shipped + AWB'd by hand), probe txns all rolled back cleanly). Client mirror `normalizeTrackingUrl` (+4 tests) — http(s)-only so `javascript:`/`data:` can't reach the customer-facing anchor. `setOrderAwb(orderId, awb, trackingUrl)` (blank link = clear); drawer card now "Courier AWB & tracking" with an optional link input + customer-visibility hint (read-only anchor when terminal); snapshot carries `trackingUrl`. **Storefront**: `MyOrderDetail.trackingUrl` — the Tracking card renders **the AWB itself as the link** ("AWB {n} ↗", gold underline, `noopener`) when a link exists, plain text + copy button otherwise. **Shipped email**: AWB anchor when linked, URL in the plain-text body (1 test → 176). tsc/176/build green.
- ✅ **6.4d (S) — AWB hidden until relevant + backward move clears it (user request 2026-07-08)**. New pure `showAwbCard(status, awb)` in `lib/admin/awb.ts` (TDD, 4 tests → 180): hidden on Pending/Confirmed, shown from Packed, and always shown when an AWB exists (data never invisible; terminal renders read-only) — drawer renders `AwbCard` behind it. **Migration 0033 (applied + live-probed, pinned by order_no)**: `admin_set_order_status` backward branch nulls `awb` + `tracking_url` (a mis-clicked ship stage's courier details are stale; re-shipping re-enters them deliberately) — probe: set AWB+link → Shipped→Packed cleared both; forward Confirmed→Packed preserved both; all rolled back. OrdersView's optimistic snapshot mirrors the clear when `next === prevStatus(prev.status)` (Cancel unaffected — not a backward step). tsc/180/build green.
- ✅ **6.5 (M) — Backward status movement before delivery** *(7)*. Same migration 0031: `admin_set_order_status` now accepts `v_ni = v_ci - 1` alongside `+1` while the order isn't terminal (Cancel branch untouched; audit trigger 0026 logs backward moves like any status change). New pure `prevStatus` in `lib/admin/order-status.ts` (+`order-status.test.ts`, 4 tests → 169 total). Drawer footer gains a secondary "← {prev}" button (hidden at Pending/terminal) wired through `onMoveBack` → the existing `runChange` (optimistic status + timeline entry). **Live probes (rolled back)**: Confirmed→Pending succeeds; Confirmed→Shipped (two steps) still raises `INVALID_TRANSITION`. tsc/169 tests/build green.
- ✅ **6.6 (M) — Print as dialog, not new tab** *(6)*. Drawer's two print buttons now open an in-app **PrintDialog**: the auth-gated `(print)` route in a **same-origin iframe** — its own toolbar (Print button, invoice↔packing-slip switch) works inside, so the shell just hosts + "Open in new tab" fallback + Escape/backdrop dismiss (`useDialog`). Dialog mounts as a **sibling of the drawer's `<aside>`** (its transform would trap `fixed` positioning). **Security relax, tightly scoped:** CSP `frame-src`/`frame-ancestors` `'none'`→`'self'` (proxy.ts) + `X-Frame-Options` `DENY`→`SAMEORIGIN` (next.config.ts) — cross-origin framing stays blocked both directions; **live-verified headers** on a local prod build. Route unchanged (still linkable/printable standalone). tsc clean; 135/135; build green.
- ✅ **6.7 (M) — Orders date filter** *(8)*. New pure `lib/admin/order-dates.ts` (TDD, 12 tests → 147): `DATE_RE`, `DEFAULT_ORDER_WINDOW_DAYS = 2`, `shiftDate` (UTC day math), `toOrderDateRange(from, to, today)` — `?from=all` → unbounded; valid `from` anchors a custom range (invalid/inverted `to` repaired to `max(from, today)`); lone/garbage params → default window (today−2 → today). `listAdminOrders` takes the resolved `range` and applies IST day bounds (`created_at >= from@00:00+05:30`, `< (to+1d)@00:00+05:30`) to the rows query AND every per-status count; `AdminOrdersPage.range` carried to the client. `orders/page.tsx` computes IST today (`en-CA`/`Asia/Kolkata`) and resolves `?from/?to`. OrdersView toolbar: two `<input type="date">` (min/max cross-clamped; clearing "from" returns to the default window) + an "All dates"/"Last 2 days" toggle link; `hrefFor` now serialises the range so tabs/pager/search all preserve it (default window is param-free URLs). Empty state says "in this date range" when a window is active. Bell "needs attention" counts stay unfiltered (admin-metrics untouched). tsc/147 tests/build green; live SQL probe deferred with the MCP down (PostgREST `gte`/`lt` on timestamptz is the same pattern 5.17's digest window verified).
- ✅ **6.8 (S/M) — Orders drawer mobile pass** *(4)*. Audited OrderDrawer at 320/375 (full-width below 440px; `px-6`×2 left 272px of content at 320). Fixes, all class-only: header gets `gap-3`, left column `min-w-0` + `break-words` order-no (20px → 22px at `sm:`), right cluster `shrink-0`; chrome padding `px-4 sm:px-6` on header/body/footer; customer phone·email + address lines `break-words`; print buttons `flex-wrap` + `min-w-[150px]` so they stack instead of clipping; both × close buttons get `-m-1.5 p-1.5` hit areas; PrintDialog title `min-w-0 truncate` + `shrink-0` actions + `whitespace-nowrap` new-tab link; timeline event column `min-w-0` and note text `break-words` (pasted URLs). Stepper left alone — 5 nowrap labels ≈ 203px + shrinkable connectors fit at 272px. tsc/build/prettier green; on-device screenshot pass deferred with the rest of the login-gated admin browser bar.
- ✅ **6.9 (M) — Realtime orders + notifications** *(1, 3)*. **Migration 0034 (applied + verified)**: `order`, `review`, `contact_message` added to the `supabase_realtime` publication via duplicate-guarded DO blocks (idempotent); `pg_publication_tables` probe confirms all three. Realtime enforces RLS per subscriber, so only admin sessions (is_admin() read policies 0006/0015/0021) receive events. New `AdminRealtimeRefresher` client island mounted in the console layout inside `AdminShell`: subscribes to `postgres_changes` (`event: *`) on the three tables via the existing browser `supabase` singleton and coalesces bursts into one 1200ms-debounced `router.refresh()` (a checkout = order + items insert + audit rows → one repaint) — orders queue, bell badge, reviews, and messages go live with zero socket-state in components; unsubscribes + clears the timer on unmount. CSP: prod `connect-src` now also lists the `wss://` twin of the Supabase origin (CSP3 maps wss→https but older Safari doesn't). Silent no-op where Realtime is unreachable (subscription just never fires; manual refresh unaffected). tsc/180/build green; live socket check rides the login-gated admin browser bar.

**Analytics & catalog**
- ✅ **6.10 (M) — Analytics: date filter + pagination** *(10, 11)*. New pure `lib/admin/analytics-range.ts` (TDD, 12 tests → 159): `toAnalyticsRange(from, to, today)` — default = last 6 calendar months (first-of-month 5 back → today, `isDefault`), custom `from` anchors a range with 6.7's repair semantics, clamped to `MAX_ANALYTICS_MONTHS = 12` (from raised to the last 12 months of `to`); `monthBuckets(from, to)` (variable IST month columns; labels gain "Nov 25"-style years when crossing a year); `rangeLabel` caption. `istDayStartIso` promoted from admin-orders into `lib/admin/order-dates.ts` (shared). Server `getProductAnalytics(range)`: buckets from the range, orders bounded `gte/lt` IST day bounds, trend generalised to floor-half vs rest (null for 1-month windows), KPI labels tagged `· 6 mo`/`· range`, `emptyData(range)` fallback. Page resolves `?from/?to/?page` (IST today). AnalyticsView: date inputs + "Last 6 months" reset in the table header (clearing `from` restores default), client-side `?page` slice (`ANALYTICS_PAGE_SIZE = 8`) over the sorted set with shared `AdminPager` (KPIs still need every row, so paging is presentation-only), sort change resets to page 1, spark bars flex (≤12 buckets fit the 104px cell), detail cards/chart caption window-aware ("Units · range", "Units · Jul", explicit date caption). tsc/159/build green.
- ✅ **6.11 (M/L) — Category images** *(12)*. **Migration 0035 (applied + live-probed, rolled back)**: `category.image_url text` + `admin_upsert_category` re-stated with image handling in both branches — blank clears, non-blank must be http(s) ≤500 (`INVALID_IMAGE_URL`; `javascript:` probe rejected; happy-path set verified). Upload pipeline DRY'd: new shared `lib/db/admin-storage.ts` (`uploadAdminImage(fd, "products"|"categories")` — 5 MB / image-only / uuid paths in the `product-images` bucket, is_admin()-gated by 0010); `uploadProductImage` now delegates (same signature, `UploadResult` re-exported); new `uploadCategoryImage` action. `CategoryInput.imageUrl` → RPC payload. CategoryModal: "Tile photo (optional)" — 64px preview (photo or heroBg gradient), hidden file input + Upload/Replace button, Remove link; URL stored on Save. CategoriesView card swatch prefers the photo (folder icon only on gradient). Storefront `CategoryTiles`: photo tiles render the image with a bottom-dark legibility gradient + cream text (motif hidden); unphotographed collections keep the exact prototype gradient tile. `getCategoryTiles` selects `*` so the column flowed via types.ts alone; existing `updateTag(categories)` on upsert invalidates the cached tiles. tsc/180/build green.

**Engagement surfaces**
- ⬜ **6.12 (M) — Contact reviewer from Reviews page** *(14)*. Validated: `review` has only `name` + nullable `user_id` (purchase-required since 0022, so new reviews carry it). Definer RPC `admin_review_contact(p_review_id)` → reviewer email (auth.users) + phone (latest order by user) — admin-gated; UI reuses the 5.15 Call/WhatsApp buttons. Legacy `user_id`-null reviews show no contact.
- ⬜ **6.13 (M) — Message resolution summary** *(15)*. Migration: `contact_message.resolution_note text`; `admin_set_message_status` requires a non-empty note when moving to Resolved; MessagesView collects it in a small dialog and displays it on resolved threads.

**Bigger rocks**
- ⬜ **6.14 (M/L) — Dashboard enrichment** *(16)*. Add: status-breakdown board, ageing-Pending list (reuse `pendingAge`), latest reviews + messages cards, top customers by revenue. Mirror the admin prototype's dashboard density (decode + render it first per CLAUDE.md).
- 🔨 **6.15 (L) — Settings-driven store info** *(17)*. **Design:** keep the existing editable scalar `setting` columns (store_name/support_email/phone/gstin, 3.11) as the contact source; add ONE `store_info` jsonb blob for the fields with no column yet (descriptor, tagline, wordmark, whatsappE164, address{line,city,state,note}, hours{short,long,note}, socials[]). `getStoreInfo()` merges scalars + blob **over** the `STORE_INFO` const (fallback + types + seed) — avoids a second place to edit name/phone/email.
  - ✅ **Slice 1 — data foundation (this turn).** Pure `resolveStoreInfo(input)` in `lib/store-info.ts` (client-safe; widens `STORE_INFO` → `ResolvedStoreInfo`, re-derives tel/mailto/wa.me from the raw handles, blank/whitespace/malformed overrides fall back to the const so the site is never blanked; `wordmark` defaults to `name.toUpperCase()`) — **TDD, 10 tests → 190 total**. **Migration 0036 (applied + live-probed)**: `setting.store_info jsonb not null default '{}'` + `admin_update_settings` gains a `store_info` write branch (whole-object replace, like banner/promo); probe wrote `{tagline,address.city}` and rolled back. Cached `getStoreInfo()` in `lib/db/settings.ts` (reads scalars + blob, `resolveStoreInfo`, `settings` tag + React.cache like `getStoreSettings`). `store_info` added to `types.ts`. tsc/190/build green. *No UI or consumer swap yet — foundation only.*
  - ✅ **Slice 2 — Settings UI (this turn).** `admin_update_settings` `store_info` write changed from whole-replace to **shallow `||` merge** (re-applied) so a partial save preserves un-edited keys — live-probed: seeding `{wordmark,socials}` then saving `{descriptor,tagline}` kept wordmark+socials. `StoreSettings` gains `storeInfo: StoreInfoFields` (flat raw strings, empty when unset) via new `readStoreInfoFields` in `getStoreSettings` (select now includes `store_info`). `settingsFormSchema` gains a `storeInfo` object (descriptor/tagline/whatsapp/address×4/hours×3, zod-capped); `settingsToFormValues`→`storeInfoValues`; `formValuesToPayload`→`store_info` sub-object (whatsapp reduced to digits, address/hours sent whole). `SettingsView` gains a **"Brand & Contact" card** (after Store Information) — each field placeholders the `STORE_INFO` default so blank = "use default". Socials/wordmark editing still deferred (preserved by the merge). Full round-trip live-probed with the real payload shape. tsc/190/build green. *Storefront still reads `STORE_INFO` directly — nothing user-visible until Slice 3.*
  - 🔨 **Slice 3 — migrate consumers** to `getStoreInfo()`. The const stays the typed fallback.
    - ✅ **3a — Footer + Header (this turn).** Storefront `layout.tsx` now fetches `getStoreInfo()` in parallel with `getStoreSettings()` and passes a resolved `info: ResolvedStoreInfo` prop to `Header` + `Footer` (both prop-driven now; one deduped fetch). Footer reads `info.wordmark/tagline/socials/name`; Header reads `info.wordmark/descriptor` and forwards `wordmark` to the client `MobileNavDrawer` (which took a `wordmark` prop — a client component can't call the server-only `getStoreInfo()`). **This is the first user-visible slice — editing tagline/descriptor/WhatsApp/address/hours in Settings now shows on the footer/header.** tsc/190/build green.
  - ✅ **Settings page redesign (operator design import, 2026-07-09).** Implemented the claude.ai/design project "Settings page redesign" (`Settings Page.dc.html`, fetched via DesignSync) as a full `SettingsView` rewrite — presentation-only, same form state/schema/action: sticky 232px section-anchor sidebar (dot-coded links to `#store-info`/`#brand-contact`/`#shipping-payments`/`#announcement-banner`/`#promo-block`, hidden below `lg`) + "Store status" card (honestly derived from the COD toggle — COD is the only tender, so off = "checkout paused"); five icon-headed section cards (38px tinted icon tiles, 14px radius, design palette #E6DECF/#EFE9DE/#FBF8F3); iOS-style switches (44×26, on `#5B1A2E`) replacing the old pills, incl. header toggles for banner/promo and payment rows for COD/Razorpay (Razorpay still disabled); live previews restyled (`#4B1122` banner bar, gradient promo block, gold `#E3B15E` highlights; dim to 45% when disabled); banner tip box; **sticky save bar** with dirty indicator (new `isDirty` flag: amber "You have unsaved changes" / green "Saved — changes are live" / grey idle, error inline). Deviations (noted in the component doc): the design's mock top bar = existing `AdminTopbar`; Playfair/Inter → the app's self-hosted heading/body fonts (CSP blocks font CDNs, 2-family budget); save bar floats inset instead of full-bleed. tsc/190/build green.
    - ✅ **3b — help/contact (user request "use db data", 2026-07-09).** `CONTACT_CHANNELS`/`SUPPORT_HOURS` consts → `contactChannels(info)`/`supportHours(info)` functions of `ResolvedStoreInfo`; contact page is async and resolves `getStoreInfo()`. Other help-content exports (editorial copy) unchanged.
    - ✅ **3c — WhatsApp builders (same turn).** `lib/whatsapp.ts` gains `EnquiryStore` (`{name, whatsappNumber}`); `whatsappUrl`/`productEnquiryMessage`/`productEnquiryUrl`/`cartEnquiryMessage`/`cartEnquiryUrl` take an optional trailing `store` defaulting to the const (existing tests unchanged). Product + cart pages fetch `getStoreInfo()` (deduped with the layout's fetch) and thread `enquiryStore` into `ProductBuyBox`/`CartView` (new optional prop each). **Bonus resolver fix**: the default WhatsApp *social badge* now re-points at the resolved number too (`defaultSocials()` in `resolveStoreInfo`, +1 test → 191).
    - ✅ **3e — metadata/SEO sweep (same turn).** `app/layout.tsx`: `metadata` const → async `generateMetadata()` (tab title/template/OG siteName from `info.name`); `buildOrganizationJsonLd(info)` (name + sameAs from resolved socials); `app/not-found.tsx` + `app/opengraph-image.tsx` (edge — builds fine with the db import) resolve wordmark/descriptor per request.
    - ⬜ **3d — email templates.** `order-confirmation`/`order-status`/`daily-digest`/`admin-alert`/`send.ts` still read `STORE_INFO` at module scope → give builders an optional resolved-info param and fetch in the senders.
    - ⬜ **3f — remaining minor readers**: `lib/legal-content.ts` + privacy/terms/refund pages, admin chrome (`AdminSidebar`/`AdminAuthShell` wordmark, `OrderPrintView` seller block). `SettingsView`'s `STORE_INFO` placeholders are intentional (they show the fallback defaults).

## Cross-cutting (ongoing, not a phase)
- ✅ **Store info config (single source of truth).** New `lib/store-info.ts` — one typed `STORE_INFO` const holding the business's identity + contact details: `name`/`wordmark`/`descriptor`/`tagline`, `phone`/`whatsapp`/`email` (each with a display form **and** a derived `tel:`/`mailto:`/`wa.me` link built from one raw handle so they can't drift), `address`, `hours` (short/long/note), `gstin` (null until issued), and `socials` (`SocialLink[]`). Consumed by `Footer` (wordmark, tagline, socials — now render as real links when a URL exists; WhatsApp badge is a live `wa.me` link; copyright name), `Header` (wordmark + descriptor), and `lib/help-content.ts` (`CONTACT_CHANNELS` phone/email/WhatsApp/address + `SUPPORT_HOURS`). Value-parity refactor (same strings) + tel/mailto/wa.me now derived. **Kept as `const`, not env** (identical across environments; YAGNI — env layering trivial to add later); **distinct from** DB-backed editable copy (banner/promo/`store_name` via `getStoreSettings`). Marketing prose/metadata that merely *mentions* the name left inline (editorial, not a maintained detail). Feeds **2.7** (WhatsApp enquiry builds from `STORE_INFO.whatsapp.number`). **tsc clean; build green (all 10 routes).**
- ✅ **Testing** (0064c58, 0435e17, 8c76de5). **Unit (67 pass / 0 fail, `bun test`):** pure domain (cart, coupons, shipping, money, whatsapp, checkout schema/order mapping) plus `submitCheckout` — the authoritative write gate — with the Supabase server client mocked: honeypot drops bots before the RPC, invalid input never reaches the DB, the RPC payload provably carries no price, success/error/unexpected-shape all mapped. **E2E (Playwright, 2 pass):** `e2e/checkout.e2e.ts` runs the critical journey (shop → product → add to cart → cart → COD form → place order → confirmation with order number → cart cleared) against a **production build** on :3200, so the strict nonce CSP + per-request rendering are exercised as shipped. Config notes: system Chrome via `channel: "chrome"` (no browser download); `*.e2e.ts` naming so `bun test` ignores them; `bun run e2e`. **Data:** E2E writes a real order into live Supabase tagged `e2e-test@example.com` — clean with `delete from "order" where customer_email = 'e2e-test@example.com'` (+ `setval('order_no_seq', 1001, false)`); this run's order was cleaned. **Visual regression (16 pass):** `e2e/visual.e2e.ts` — full-page screenshots of home / shop / product / empty-cart at 320/768/1024/1440; baselines committed (`visual.e2e.ts-snapshots/`, ~7.8 MB); `toHaveScreenshot` defaults in config (animations disabled, 2% diff tolerance); verified stable across two fresh runs; regenerate with `bun run e2e -- --update-snapshots`. **Coverage (`bun test --coverage`):** 100% funcs / 99.78% lines across the tested domain + action layer (actions, cart, checkout order/schema, coupons, shipping, store-info, money, whatsapp) — exceeds the 80% target. Caveat: Bun reports only test-imported files; `lib/db` row-mappers, `stores/cart`, and components are exercised via E2E + visual regression instead (per web testing rules: visual regression > brittle markup assertions for visual components). **Deferred to deploy:** firefox/webkit projects (need `playwright install` browser downloads) and a staging/branch Supabase so E2E stops writing prod data.
- ✅ **Performance** — CWV budgets (LCP <2.5s), `next/image` dims, font preload, bundle budget. *Done in 4.18 (see its entry for the caching layer + measurements).*
- ✅ **Security** (d0f32fe). Defense-in-depth for the storefront. **Nonce CSP** (`proxy.ts`, Next 16's successor to `middleware.ts`): a fresh Web-Crypto nonce per request; `script-src 'self' 'nonce-…' 'strict-dynamic'` (no `'unsafe-inline'` for scripts), `img-src`/`connect-src` scoped to the Supabase origin, `frame-ancestors`/`frame-src`/`object-src 'none'`, `base-uri`/`form-action 'self'`, `upgrade-insecure-requests`. **Static headers** (`next.config.ts` `headers()`): HSTS (2y, preload), `X-Content-Type-Options: nosniff`, `X-Frame-Options: DENY`, `Referrer-Policy: strict-origin-when-cross-origin`, `Permissions-Policy` (camera/mic/geo/topics off). **Force-dynamic tradeoff:** the root layout reads a request header so every route renders per-request — required for Next to stamp the nonce onto its bootstrap scripts (a static page's baked HTML can't carry a per-request nonce, which would otherwise block all its JS). All routes are now `ƒ`; **revisit caching in the Performance pass.** **Form anti-abuse:** a `Honeypot` decoy field (off-screen, `aria-hidden`, `tabIndex=-1`, `autoComplete=off`) on checkout; the `submitCheckout` action drops any submission with a non-empty honeypot before touching the DB, kept out-of-band from the shared `checkoutSchema`. **Audit findings (already-good):** no service-role key anywhere (only `NEXT_PUBLIC_*`; both browser + server clients use the RLS-safe publishable key); the action re-validates the same Zod schema server-side and never sends prices (RPC recomputes totals); no `dangerouslySetInnerHTML`; fonts self-hosted via `next/font` (no CDN in CSP). **Verified:** build green (all routes dynamic), header nonce == script nonce within a request, and home/checkout/shop/product load with **zero CSP console violations** (images + hydration intact). **Deferred:** IP/rate-limiting needs an edge store (e.g. Upstash) — add at deploy. The newsletter footer form is UI-only (no action) so needs no honeypot yet.
- 🧹 **Database reset to fresh-deploy state (2026-07-08).** At the operator's request, wiped all app data on project `naolegptozpaiojozzcy` to a clean "just deployed" baseline (schema/RPCs/RLS/realtime publication untouched). One atomic txn: `truncate ... restart identity cascade` across all 15 public tables (order/order_item/review/contact_message/admin_audit_log/admin_role_audit/subscriber/customer_profile/product_image/product_option/product/category/coupon/setting/app_secret — all were already empty); `alter sequence order_no_seq restart with 1001` + `contact_message_no_seq restart with 1` (next IDs `JR-YYMMDD-1001` / `TK-YYMMDD-001`); `delete from auth.users` (both accounts — the E2E admin + a test customer). **Admin re-bootstrapped**: operator re-signed-up `chetangkajjidoni+e2e@gmail.com`, promoted to admin by directly stamping `raw_app_meta_data` `role='admin'`+`role_granted_at` (the `admin_grant_role` RPC can't bootstrap the *first* admin — its `is_admin()` gate needs an existing one) + an `admin_role_audit` `grant` row. **⚠️ Two learnings for a real fresh deploy:** (1) **`setting` is a singleton** — `admin_update_settings` (0018) is `UPDATE ... WHERE id = true`, no insert, and raises `SETTINGS_ROW_MISSING` when the row is absent; truncating it broke Settings saves until reseeded with `insert into setting (id) values (true)` (all columns default; `store_name='JR Jewellers'`, free-ship ₹999, flat ₹79, cod on, banner/promo `{}`). **A deploy that runs migrations alone gets NO settings row → add this seed to the deploy/seed step (candidate for 6.15 / a `supabase/seed.sql`).** (2) Storage rows can't be deleted via SQL (`storage.protect_delete` trigger) — one orphaned image in the `product` bucket remains; clear via the dashboard/Storage API only. Catalog (products/categories/coupons) intentionally left empty — operator adds via the console (no seed migration exists).
- ⏸️ **Deploy** — Vercel (project + env vars) once Phase 1 is reviewable. *Tracked as 4.13 (plus OAuth enable, email templates, Site URL).*

---

### How we work this list
1. Pick the **top unchecked** task. 2. I implement it (small diff, mirrors plan/tokens). 3. Mark ✅, note any follow-ups. 4. Next.

### Open questions (decide as we hit them)
- **Product imagery:** seed `product_image` rows have NO `url` — only `design_name` + a CSS gradient `bg` (prototype rendered gradient cards, not photos). Decide: (a) ship gradient placeholders for v1 and render from `bg`, or (b) source real photos and populate `url` (then `primary_image_url` auto-fills). If (a), we may also denormalize `bg` onto `product` to keep listings join-free.
- Guest checkout only, or optional customer accounts in v1.
- Confirm Vercel as host (assumed).
