# JR Jewellers — environment template. Copy to .env.local and fill in.

# Supabase (storefront, public client config)
NEXT_PUBLIC_SUPABASE_URL=https://<project-ref>.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_xxx

# Absolute site origin (sitemap/robots/metadataBase/JSON-LD) — optional,
# falls back to http://localhost:3000 in dev. Set to the real domain at deploy.
NEXT_PUBLIC_SITE_URL=https://your-domain.com

# Server-only (added in later phases — never expose to the browser)
# SUPABASE_SERVICE_ROLE_KEY=        # admin writes (Phase 3)
# RAZORPAY_KEY_ID=                  # payments (Phase 5)
# RAZORPAY_KEY_SECRET=
# SHIPROCKET_EMAIL=                 # fulfilment (Phase 5)
# SHIPROCKET_PASSWORD=
