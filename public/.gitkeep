# Keeps the Next.js `public/` directory tracked in git even while empty.
# The Docker build's `COPY --from=builder /app/public ./public` (Dockerfile)
# needs this dir to exist in a fresh clone, e.g. Dokploy's deploy checkout.
